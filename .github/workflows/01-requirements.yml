# Stub workflow — trigger and steps will be replaced when the Stage 1 agent is built.

name: FORGE Stage 1 — Requirements

on:
  workflow_dispatch:  # manual trigger for now — real triggers added later

jobs:
  requirements:
    runs-on: ubuntu-latest
    steps:
      - name: Guard clause
        run: |
          echo "Workflow stub only — agent logic not yet implemented"
          exit 0
