# FORGE — Python dependencies
# Install with: pip install -r requirements.txt

# HTTP
requests>=2.31.0

# GitHub App JWT auth
PyJWT>=2.8.0

# GitHub API client (used by github_helper.py — see module docstring for rationale)
PyGithub>=2.1.1

# Excel parsing
openpyxl>=3.1.2

# YAML
PyYAML>=6.0.1

# Environment variable loading for local dev
python-dotenv>=1.0.0

# Anthropic Python client — direct Messages API calls for all stages except Stage 3.
# Stage 3 (Implementation) uses managed_agents_wrapper.py with raw requests for the
# Managed Agents beta endpoints, which are separate from the Messages API.
# ADR-0011: switched from claude-agent-sdk to this package; see that ADR for rationale.
anthropic>=0.120.2

# Semgrep — SAST scanner for the Security Agent (Stage 5). Gitleaks and OWASP
# Dependency-Check are NOT pip-installable — separate binary installs, see CLAUDE.md.
semgrep>=1.90.0
