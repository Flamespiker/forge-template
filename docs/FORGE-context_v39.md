# FORGE — Project Context & Decision Log

**Project:** FORGE — Full-SDLC Orchestration with Review Gates for Engineers  
**Owner:** Mike Faulkner (mfaulkner@legalaid.ab.ca) — Legal Aid Alberta  
**Last Updated:** 2026-08-05 (Phase 4, step 4.8 complete — branch protection live on `forge-demo-apps` `main`; 4.10 remains open — chat 37)  
**Purpose:** Living reference document. Read this at the start of every new chat to restore full project context without re-explanation.

---

## Terminology Note

Two meanings of "session" now exist in the FORGE world. To avoid confusion:

| Term | Meaning |
|------|---------|
| **Chat** or **chat thread** | One Claude.ai conversation — one per document being written (project management) |
| **Agent session** | A running Managed Agents API instance (`/v1/sessions/:id`) — used in Stage 3 |

Documents that discuss Managed Agents always say "agent session" for the API concept. "Chat" or "chat thread" is used for the project management concept throughout this document.

---

## What is FORGE?

FORGE is an AI-orchestrated SDLC platform that takes business requirements from a BA-produced Excel spreadsheet, moves them through a fully staged software development lifecycle, and deploys a working application — with humans only reviewing and approving at defined gates. It is built as a GitHub template repository that developer teams at Legal Aid Alberta clone and manage as their own "orchestration manager" instance.

The core pattern: **deterministic orchestration** (Git operations, state transitions, PR creation) paired with **bounded agent execution** (Claude-powered agents producing artifacts at each stage). Agents do the work. Humans approve the outcomes.

---

## Key Decisions Made

### Intake & Requirements
- Business requirements are gathered **manually by a BA** using a standard Excel spreadsheet template
- The spreadsheet has a dedicated **overview/context tab** (a template the BA fills out: audience, purpose, problem being solved, etc.)
- The Requirements Agent reads the completed spreadsheet, asks a focused set of clarifying questions (5–7 max, one follow-up round if needed), then produces structured requirements
- Requirements are **confirmed by a human before ADO work items are created** — agent produces a draft first
- The ADO integration creates **Epics → Features → User Stories** from the approved requirements
- Full traceability chain: spreadsheet row → ADO User Story → GitHub branch → PR → deployment
- **Intake upload mechanism:** Two options — issue attachment (recommended for build phase; simpler for BAs who are not comfortable with Git) or repository path (`intake/<request-id>.xlsx` via PR). Team-layer choice — Orchestration Manager documents which one the team uses.
- **"Clarification complete" signal:** The BA applies the label `clarification-complete` to the tracking issue when finished answering the agent's questions. Not a keyword reply. If a second clarification round is needed, the BA removes the label, answers, and re-applies it.
- **Excel Intake Template structure:** Three tabs — Instructions (how-to guide, colour legend), Overview (six sections: Request Identification, Request Type, Problem & Purpose, Success Criteria & Scope, Constraints & Considerations, Additional Context), Requirements (one row per requirement with columns: Req #, Type, Priority, User Story / Requirement, Acceptance Criteria, Notes / Constraints). Yellow cells = BA input. Four example rows pre-populated and clearly labelled as examples to replace/delete before submitting.

### Technology Stack
- **Source control & CI/CD:** GitHub (repos + GitHub Actions pipelines)
- **Work item tracking:** Azure DevOps (ADO) Boards — Epics, Features, User Stories
- **Agent runtime:** Anthropic Claude Managed Agents (Stage 3 coordinator/subagent pattern); base `anthropic` Python client / Messages API for all other stages (ADR-0011, chat 21 — supersedes the original Claude Agent SDK decision; Documents 2/3/9 correction still outstanding, see below)
- **Primary model:** Claude (Sonnet tier for non-Stage-3 stages; Opus tier for coordinator in Stage 3 — see ADR-0010)
- **Frontend:** React / Next.js — **TypeScript mandated (core layer)**
- **Backend:** .NET
- **Architecture pattern:** C4 model, microservices (small, manageable domains for mid-sized org)
- **Development methodology:** Spec-driven development
- **Containerization:** Docker, targeting **Azure Container Apps** (migration path from Azure App Service)
- **Current hosting:** Azure App Service (org), personal Azure account (build phase)
- **Anthropic API:** Mike's personal developer API account (build phase), org API account (production)
- **GitHub account (personal, build phase):** `Flamespiker` — owns both `forge-template` and `forge-demo-apps`
- **ADO org (build phase):** `spike99` (`https://dev.azure.com/spike99`) — **note:** this is a different identity than the GitHub account; the two got confused once during Phase 2 build (see chat 20 session note) and are worth keeping straight going forward

### Platform Model
- FORGE is a **two-layer platform:**
  - **Core layer (locked):** Security gates, ADO work item structure, naming conventions, branching strategy, deployment standards, Excel intake template format. Standardized across all teams. Not customizable.
  - **Team layer (customizable):** Tech stack specifics, agent personas, additional tools within approved list, notification channels, team-specific skills.
- Delivered as a **GitHub template repository** — teams clone it and own their instance
- Teams can pull core platform updates via the template repo pattern
- The person who owns and maintains a team's FORGE instance is called the **Orchestration Manager** — a developer or tech lead role

### Governance
- Core platform changes require a lightweight **RFC (Request for Comments)** process using GitHub Discussions in the FORGE template repo
- Decisions are recorded as **ADRs (Architecture Decision Records)** in `core/decisions/`
- Decision authority: Core Platform Owner (final approval) + Technical Reviewers (named per RFC) + Orchestration Managers (proposers)
- RFC timeline: 3 business days to assign reviewer, 10 business days review period, 30 days to implement accepted RFCs
- Breaking changes go through RFC with `breaking-change` label; non-breaking bug fixes go direct as PRs
- Team-layer changes need no RFC — Orchestration Manager has full authority within core-layer boundaries
- Nine seed ADRs will be written into `core/decisions/` at initial repo setup (see Document 4)
- **ADR-0010 added** — Managed Agents for Stage 3 implementation coordinator (see Architecture Decisions below)
- Core layer updates are versioned (semantic versioning); teams apply within 30 days (non-security) or 10 days (security gate changes)

### Human Confirmation Gates
Every stage has a human gate before the next stage begins:
1. Requirements — review draft ADO work items, approve before creation
2. Design — review architecture doc and API contracts, approve PR
3. Code — review implementation diff, agents have pre-flagged issues
4. QA sign-off — review test report and bug list
5. Security sign-off — review severity-tagged inline PR comments
6. Deploy approval — one-click GitHub Environment approval

PRs are always **opened by the agent, confirmed/approved by a human** — never the reverse, and no agent merges its own PR.

### Enhancement Workflow
- FORGE handles both **greenfield apps** and **enhancements to existing codebases**
- Enhancements require a **Stage 0 "codebase ingestion"** step — the agent reads the existing repo, understands architecture and conventions, identifies where the change fits — before the Requirements Agent begins
- With the repo model below, "greenfield" means a **new folder under `services/`** in the existing monorepo, not a new repository; "enhancement" means modifying a folder that's already there. Both use the same requirements/design templates — enhancement just adds the ingestion summary as an upstream input.

### Scope & Demo Plan
1. **App 1:** Small greenfield app — validates the full pipeline end-to-end
2. **App 2:** Second greenfield app — proves repeatability
3. **Enhancement:** A targeted enhancement to App 1 or App 2 — proves the enhancement workflow

### Security Approach
- Practical, not regulatory-heavy (LAA is not under heavy AI governance obligations)
- Security gates embedded in the workflow (SAST, secrets detection, OWASP checks)
- Data handling basics and responsible AI principles covered in training
- Client data sensitivity noted as a callout in the AI Foundations Guide

### Training Philosophy
- Two tracks:
  - **Track 1 — AI Foundations:** What LLMs are, how agents work, agentic orchestration concepts, prompt engineering basics, context window awareness, responsible AI, practical AI governance
  - **Track 2 — Orchestration Manager:** Setup, customization boundaries, agent skill management, failure handling, governance process participation
- Includes recommendations for external product training and AI certifications developers should complete
- **Document 5 (AI Foundations) requires updating** to add Claude Cookbooks, Managed Agents quickstart, and multi-agent docs — see "Documents Requiring Updates" below

---

## Architecture Decisions (Document 2)

### Orchestration model
- **Event-driven via GitHub Actions** — no separate long-running orchestrator service. GitHub Actions workflows, triggered by PR/label/issue events, *are* the deterministic orchestration layer.
- **State lives natively in GitHub** — issue labels, PR review state, GitHub Environments with required reviewers. No bespoke state database.
- **Agent invocation is stateless per stage** — each stage (except Stage 3) spins up a fresh Claude Agent SDK call scoped to that stage's inputs/outputs. No persistent cross-stage agent memory; context passes forward as committed files in the target repo. (ADR-0002 — see clarification note below.)
- **ADR-0002 clarification (from ADR-0010):** "Stateless per stage" means no cross-stage memory. Within Stage 3, the Managed Agents coordinator session maintains coordinator-to-subagent state during execution, bounded inside the stage window. The stage still starts from committed files and ends by committing files. The principle is not violated — it is clarified.
- Every workflow opens with a **guard clause** confirming its precondition label is present, to prevent stray events from re-triggering a stage out of order.

### Managed Agents adoption for Stage 3 — ADR-0010

**Decision:** Stage 3 (Implementation) uses an Anthropic Managed Agents coordinator agent with three specialist subagents (Backend, Frontend, Test Writer) rather than three independent GitHub Actions parallel jobs.

**Why:** The parallel-jobs approach required an explicit integration-check job to catch branch commit conflicts between three agents writing to the same feature branch concurrently. The Managed Agents coordinator pattern eliminates this — the coordinator runs the three subagents in parallel on a shared sandbox filesystem, synthesizes their outputs, handles integration naturally, then commits the complete implementation as a single coherent unit and opens the draft PR.

**What changes:**
- The Stage 3 GitHub Actions workflow now invokes one Managed Agents coordinator agent session instead of three parallel jobs
- The integration-check job is eliminated — coordination is native to the Managed Agents session
- Claude Console provides a per-subagent audit trail for Stage 3 in addition to GitHub's audit trail
- Billing: Standard token rates **plus $0.08 per agent session-hour** of active runtime

**What does not change:**
- All other stages remain as standalone stateless Claude Agent SDK calls invoked within GitHub Actions jobs
- GitHub Actions is still the deterministic orchestration layer for all state transitions and gates
- The human gate at Stage 3 (Technical Approver reviewing the draft PR) is unchanged
- The no-self-merge rule applies — the coordinator opens the PR; a human approves and merges it

**Affected documents requiring update:**
- Document 2 (Architecture) — Sections 2.2, 3, 4.5; ADR-0002 clarification note — ✅ done, chat 12
- Document 3 (Tool Inventory) — Managed Agents billing row in Anthropic section — ✅ done, chat 14
- Document 4 (Governance) — ADR-0010 added to seed ADR list (10th ADR) — **done** (completed chat "FORGE 04 Governance," 2026-07-23; see drift-correction note under chat 22 below — this was miscarried as "still outstanding" in every session note from chat 13 through chat 21)
- Document 5 (AI Foundations) — Section 9 training resources; Section 3 orchestration concepts — ✅ done, chat 15
- Document 6 (Orchestration Manager Guide) — Part 4 failure handling (Managed Agents failures) — ✅ done, chat 16
- Document 7 (Customization Reference) — Agent Configuration section note — ✅ done, chat 16 (scope was larger than "note" — see session note)
- Build Plan — Phase 3 steps 3.5–3.7 become subagent definitions; new step 3.4a coordinator — ✅ done, chat 13
- Document 9 (README) — Minor note on Stage 3 — ✅ done, chat 17 (turned out to be three factual corrections, not just a note — see session note)
- Document 1 (Product Specification) — originally scoped as "No — stage mechanics don't change"; review in chat 18 found Section 3.3 omitted the Implementation Coordinator and misattributed the branch-commit/PR-open action to the subagents — ✅ done, chat 18

**ADR-0010 full text:**

```
# ADR-0010: Anthropic Managed Agents for Implementation Stage Coordination

Status: Accepted
Date: 2026-07-22

Context:
FORGE's implementation stage (Stage 3) requires Backend, Frontend, and
Test Writer agents to run in parallel on the same feature branch. The
original design used three independent GitHub Actions jobs writing
concurrently to the branch, with a separate integration-check job to
catch merge conflicts. Anthropic shipped Managed Agents multi-agent
orchestration (public beta, May 2026) — a native coordinator/subagent
pattern on a shared sandbox filesystem.

Decision:
Stage 3 uses a Managed Agents agent session: one coordinator agent
declares Backend, Frontend, and Test Writer as specialist subagents.
The coordinator runs them in parallel on a shared sandbox filesystem,
synthesizes their results, handles integration checking natively, then
commits the complete implementation to the feature branch and opens the
draft PR. All other stages remain as standalone stateless Claude Agent
SDK calls invoked within GitHub Actions jobs.

ADR-0002 (stateless per stage) remains valid. Stateless means no
cross-stage memory. Within Stage 3, the Managed Agents agent session
maintains coordinator state during execution, bounded by the stage
window. The stage starts from committed files (design.md, openapi.yaml,
tasks.md) and ends by committing files (feature branch). The principle
is not violated — it is clarified.

Consequences:
+ Eliminates branch commit race conditions between parallel agents
+ Integration check is native to the coordinator, not a separate job
+ Claude Console provides per-subagent audit trail for Stage 3
+ Cleaner failure isolation — one agent session failure vs. three job failures
+ Coordinator can check in on subagents mid-workflow
- Managed Agents is beta (managed-agents-2026-04-01 header required)
- Billing: $0.08/agent session-hour active runtime in addition to token costs
  — track actuals during App 1; update Document 3 cost summary
- Managed Agents failure modes differ from standalone job failures —
  Orchestration Manager Guide needs a specific section for this
- Managed Agents multi-agent orchestration is public beta — monitor for
  breaking changes during build phase
```

### Managed Agents API — build-phase notes (confirmed Phase 2.9, 2026-07-25)

These are hands-on findings from the Phase 2.9 access check, worth building correctly into the Phase 3 Managed Agents API wrapper (step 3.1) rather than rediscovering them then:

- Beta header `managed-agents-2026-04-01` confirmed accepted on the personal developer API key used for build phase.
- Full lifecycle verified end-to-end: agent create → environment create → session create → send message via events endpoint → poll to idle → archive (session → agent → environment).
- Test model used: `claude-sonnet-4-6` (confirmed compatible with Managed Agents beta as of this date). This was only a connectivity test model choice — not necessarily the production model for the Stage 3 coordinator (ADR-0010 specifies Opus for the coordinator, Sonnet for subagents — apply that split when Phase 3 builds the real Implementation Coordinator).
- **Events endpoint body shape:** sending a message requires
  ```json
  {
    "events": [
      {"type": "user.message", "content": [{"type": "text", "text": "..."}]}
    ]
  }
  ```
  A flat `content` field at the top level is rejected with a 400 (`unknown field "content"`).
- **Archive race condition observed:** a session can flip briefly back to `running` immediately after reaching `idle` (e.g., trailing extended-thinking wrap-up), causing an archive call to fail with a 400 ("cannot be archived while its status is running") even though the poller had just seen it idle. Build a short retry-with-backoff around session archival in the real wrapper rather than treating this as a hard failure.
- Two throwaway local diagnostic scripts (`managed_agents_check.py`, `archive_retry.py`) were created for this check — gitignored, not committed to `forge-template`, not intended for reuse beyond this one verification.

### Base Anthropic client for non-Stage-3 invocation — ADR-0011

**Decision:** The six non-Stage-3 stages (Codebase Ingestion, Intake, Requirements, Design, QA, Security, Deploy) switch from the Claude Agent SDK to the base `anthropic` Python client, calling the Messages API directly. Stage 3 is unaffected (Managed Agents, ADR-0010, separate mechanism).

**Why:** Phase 3.1 live testing showed every Agent SDK invocation — even a trivial, tool-free text exchange — pays a fixed cost: ~25,700 tokens of Claude Code's bundled system prompt/tool definitions written to cache on a cold call (~$0.10 at $3.75/MTok cache-write rates), plus a subprocess-launch latency floor measured at ~10 seconds regardless of task size. None of the six stages ever use the SDK's autonomous tool-execution capability — FORGE's deterministic Python layer handles all file I/O; Claude only generates text from content already in the prompt. Confirmed empirically: every Phase 3.1 invocation across these stages already passed `allowed_tools=[]`.

**What changes:**
- `claude_agent_wrapper.py` calls the Messages API directly instead of the Agent SDK's `query()`
- `invoke_agent()` drops the `allowed_tools` parameter (no tool-use loop left to scope) and gains an explicit `max_tokens` parameter (the SDK managed this internally; the raw Messages API requires it)
- `total_cost_usd` is now computed via a maintained per-model rate table in the wrapper, rather than read directly from an SDK-provided `ResultMessage` — needs manual updates if Anthropic's pricing changes
- `requirements.txt`: `anthropic` becomes a direct dependency again; `claude-agent-sdk` is removed (Stage 3's Managed Agents wrapper never depended on it — confirmed, uses raw `requests` to the beta REST endpoints)

**What does not change:**
- `invoke_agent()`'s core signature and `AgentResult`'s shape are preserved as closely as possible — the stage-agent scripts calling it need no architectural changes, only the wrapper's internals changed
- Stage 3 / Managed Agents is entirely unaffected

**Status as of this chat's end: decided, code rewrite handed to Claude Code, NOT yet confirmed complete or re-tested.** Do not assume this is live until a real diff and a clean `smoke_claude_agent` re-run have been reviewed.

**ADR-0011 code rewrite — VERIFIED, chat 22.** Real `git diff` reviewed across `claude_agent_wrapper.py`, `requirements.txt`, `smoke_claude_agent.py`, and `CLAUDE.md`. Real smoke test output reviewed verbatim (5/5 passed: output_text, latency, token counts, total_cost_usd all real and positive; `total_cost_usd: 0.00021` on 30 input / 8 output tokens against Sonnet 4.6 — arithmetic confirmed: 30×$3.00 + 8×$15.00 = $0.00021, matches the response exactly). `_MODEL_RATES` table independently checked against Anthropic's live pricing page (2026-07-29) — Sonnet 4.6, Opus 4.6, and Haiku 4.5 rates all match published pricing exactly, nothing fabricated. Error-handling design confirmed honest: `invoke_agent()` has no try/except around `client.messages.create()`; `is_error=False` is a compile-time constant, not a runtime check; API failures propagate as exceptions. This is documented accurately in both the function docstring and CLAUDE.md — not glossed over.

**Affected documents requiring update — gate cleared, ready to action:**
- ~~Document 2 (Architecture) — agent invocation section currently states Claude Agent SDK for all non-Stage-3 stages~~ — **done**, produced `02-forge-architecture-document-v3.md` (see Document List; exact changes not re-summarized here — see the file's own diff from v2)
- ~~Document 3 (Tool Inventory) — §3.3 Claude Agent SDK row and cost summary~~ — **done**, produced `03_FORGE_Tooling_v6.md`, plus the flagged open item (Sonnet 5 introductory pricing as a future cost-optimization candidate) added. **Follow-up, chat 24:** the dollar estimate itself (~$1–5) had been left unrevised despite the invocation-mechanism fix — caught during the Document 9 refinement pass and corrected to ~$0.50–3 in `03_FORGE_Tooling_v7.md` (see chat 24 note below).
- ~~Document 9 (README) — Prerequisites and cost-reference sections reference the Agent SDK~~ — **done, chat 23 (this chat).** Actual review found the flagged assumption didn't hold: Document 9's Prerequisites bullet (`Anthropic API — an API key with Managed Agents beta access...`) never named the Claude Agent SDK, and the Cost reference table was already split by ADR-0010 mechanism (API tokens vs. Managed Agents runtime) rather than by invocation library — so neither section was actually factually wrong, unlike Docs 2 and 3, which did name the SDK explicitly and required real corrections. The one genuine gap: the reader-relevant *consequence* of ADR-0011 (elimination of the ~$0.10 cold-call cost + ~10s launch latency the Agent SDK's bundled CLI added per invocation) wasn't mentioned anywhere. Added a single footnote under the Cost reference table stating this, worded to match Document 3's ADR-0011 note. No other section changed. Also investigated whether the Prerequisites' "Node.js 20+ installed locally" line was Agent-SDK-driven (it would have been a genuine correction if so) — confirmed via Documents 2/3/6 that Node.js only appears in this document set in connection with `actions/create-github-app-token`'s own GitHub-hosted-runner requirement (not a local prerequisite) and with the target application's own TypeScript/Next.js stack — unrelated to the Agent SDK, so left unchanged. Produced `09-forge-readme_v4.md` (project file was already at v3 for reasons not reflected in this context doc's session log — treated v3 as the correct current baseline and incremented from there).
- FORGE Build Plan — step 3.1's SDK-wrapper line; Phase 8.3's "ten seed ADRs" count needs to become eleven, and a step should track ADR-0011's actual commit to `core/decisions/`; this would be v4 — **done, chat 25 (this chat).** Step 3.1's wrapper description rewritten for the base `anthropic` Messages API client, plus the Document-6 try/except hard requirement folded in as a callout. New step 3.1a added to track committing ADR-0011 itself to `core/decisions/`. Phase 8.3 reworded rather than a blunt "ten→eleven" swap — resolved a real internal inconsistency in this context doc's own prior notes: one earlier note called ADR-0011 "an organic addition BEYOND the original ten-seed-ADR list" while this affected-documents entry said the count "needs to become eleven." Both are true depending on what's being counted — ADR-0011 isn't one of the original Phase-1 seed stubs, but by Phase 8 (Handoff Readiness) the actual ADR count in `core/decisions/` is eleven. Phase 8.3 now says so explicitly rather than picking one framing and burying the other.
- ~~Document 6 (Orchestration Manager Guide) — added chat 22, not in the original ADR-0011 list.~~ Needs a new, explicit requirement: every stage-agent script from 3.2 onward MUST wrap `invoke_agent()` in try/except at the call site. — **done**, produced `06_Orchestration_v3.md`

**ADR-0011 full text:** see `ADR-0011.md`, generated chat 21. **Committed to `core/decisions/0011-base-anthropic-client.md` in chat 26** (step 3.1a) — reformatted to the standard ADR template (Status/Date/RFC front matter + `## Context`/`## Decision`/`## Consequences` headers). RFC field recorded as N/A — decided organically during Phase 3.1 build/smoke-testing, no GitHub Discussion was opened; recorded as a full ADR rather than a documentation-only fix because it reverses architecture stated in Documents 2, 3, and 9.

### Stage-agent implementation conventions — Phase 3.2/3.3 build notes (chat 26, 2026-07-29)

Cross-cutting conventions established while building the first two real stage-agent scripts (Intake, Requirements). Future stage scripts (Design onward) should follow these rather than reinventing them:

- **Agent-comment marker.** Every comment a FORGE agent posts to the tracking issue is prefixed with an invisible HTML comment: `<!-- forge:agent-comment stage=<stage> request_id=<id> -->`. This is how a later stage (first needed by Requirements, reading Intake's questions vs. the BA's answers) tells its own/another agent's comments apart from a human's reply. Login/account-based detection was considered and rejected — in local-dev testing the same GitHub account posts both the agent's comment (via `GITHUB_TOKEN`) and the simulated BA's reply, so it wouldn't distinguish anything; in production the agent would show as `github-actions[bot]`, but the marker approach works identically in both contexts without a separate code path. Established on `intake_agent.py`, adopted by `requirements_agent.py`.
- **Shared formatting helpers now live in `file_io.py`**, not duplicated per stage script: `format_overview_markdown(overview)` and `format_requirements_markdown(requirements)` render the dicts/lists `read_xlsx()` returns as prompt-ready Markdown. Originally built as private functions inside `intake_agent.py`; moved and made public once `requirements_agent.py` needed the same rendering.
- **`github_helper.py` gained `get_issue_comments(issue_or_pr_number)`** — same auth pattern as `post_comment`/`add_label` (GITHUB_TOKEN, same-repo on `forge-template`). Needed by Requirements Agent to read the BA's clarification answers back off the tracking issue thread; Build Plan step 3.3 assumed this capability existed, but it didn't until this chat.
- **Requirements stage commits directly to `main` in the monorepo — no branch, no PR.** This is a deliberate, real exception to ADR-0009's general "agents open PRs; humans approve" framing, followed because Document 6's Gate 1 description is more specific and doesn't mention a PR (only "review requirements.md and a summary comment... apply `requirements-approved`"), and the branching list only defines `feature/<request-id>` and `design/<request-id>` — no requirements-stage branch. Worth tightening ADR-0009's wording eventually so it doesn't read as a blanket rule it isn't. Files land at `docs/<request-id>/requirements.md` and `docs/<request-id>/ado-work-items.json` via `commit_files(branch_name="main", ...)`.
- **Draft ADO payload shape mirrors `ado_helper.py`'s call order directly**, rather than inventing a placeholder-ID scheme: `{"epic": {...}, "features": [{"title", "description", "user_stories": [{"title", "description", "acceptance_criteria", "source_req_number"}]}]}`. Whatever eventually creates the real ADO items (Phase 4, step 4.3) can walk this tree top-down and feed each level's real returned work-item ID as the next level's `parent_*_id` — no invented reference IDs needed at draft time.
- **Requirements Agent uses one structured-JSON `invoke_agent()` call**, not two, to produce both `requirements_markdown` and `ado_payload` together — cheaper, and guarantees the human-readable doc and the ADO hierarchy describe the same requirements rather than risking drift between two independently-generated artifacts.
- **Two real bugs found via dry-run testing, both now fixed in shared code (affect every future stage that touches these paths):**
  1. `read_xlsx()`'s Overview parsing didn't strip the template's bracketed instructional placeholder text (e.g. `"[Example: FORGE-2026-001 ...]"`) from unfilled cells — it was read as literal BA input. Fixed: any Overview value starting with `[` and ending with `]` is now treated as blank.
  2. Any agent script printing emoji/unicode to stdout (e.g. `--dry-run` mode) could throw `UnicodeEncodeError` on a Windows console using a legacy codepage. Fixed: `main()` now calls `sys.stdout.reconfigure(encoding="utf-8")` as its first line.

### Stage-agent implementation conventions — Phase 3.4 build notes (chat 27, 2026-07-29)

Cross-cutting conventions established while building the Design Agent, the first stage script to read a file another agent previously committed to the monorepo rather than a local input. Future stage scripts (QA onward) should follow these rather than reinventing them:

- **`github_helper.py` gained `get_file_contents(path, branch="main")`.** Same App-installation-token auth context as `create_branch`/`commit_files`/`open_pr` (cross-repo into `forge-demo-apps`), added because the Design Agent needs to read `docs/<request-id>/requirements.md` — an artifact the Requirements Agent committed to the monorepo, not something available in a local checkout of `forge-template`. This is the same category of gap as `get_issue_comments()` being missing before the Requirements Agent needed it (chat 26): a foundational retrofit needed before the stage script could be written, not a bug in the stage script itself. `smoke_github.py` extended to 8 checks — reads back the file the smoke test's own `commit_files` call just wrote, verifying the round-trip.
- **`team/stack-preferences.yaml`'s team-choice fields are still literal template placeholders in the live repo** — confirmed by fetching the file directly rather than assuming Phase 1.4 filled them in. Only the core-layer mandates (TypeScript, Next.js, .NET, Docker, Azure Container Apps, GitHub Actions) and `api_style: rest-openapi` hold real values; `css_approach`, `component_library`, `state_management`, `orm`, `logging`, and `monitoring` all still read their placeholder text (e.g. `your-css-approach`). Same underlying problem as the bracket-placeholder bug found in Step 3.2's Overview parsing, just a different literal marker. **Fixed the same way, adapted to the marker:** `file_io.py` gained `format_stack_preferences_markdown(prefs)`, which treats any string value starting with `"your-"` as not-yet-decided rather than a real team choice, and renders it to the model as explicitly unset. The Design Agent's system prompt instructs it to propose a sensible default for any such field and flag the proposal as a recommendation for the Technical Approver to confirm at Gate 2 — never present an unset field as if it were an existing team standard. Confirmed working on the real file: the live run flagged exactly the six unset fields (Tailwind/shadcn/ui/TanStack Query proposed for frontend, EF Core for the ORM, Application Insights for logging/monitoring) and none of the seven real values.
- **Design Agent uses one structured-JSON `invoke_agent()` call**, producing `design_markdown`, `openapi_yaml`, and `tasks_markdown` together — same "one call, not several" reasoning as Requirements Agent (cheaper, keeps the three documents mutually consistent rather than risking drift between independently-generated artifacts).
- **`openapi_yaml` is validated with `yaml.safe_load()` before committing** — catches a malformed spec at the point of generation rather than committing broken YAML to the monorepo. Live run's output parsed cleanly on the first attempt.
- **`_MAX_TOKENS = 20000`** for the Design Agent (vs. 8000 for Requirements) — three documents, including a full OpenAPI spec, in one response. Live run used 12,738–12,745 output tokens with a clean `end_turn`, so there's headroom; worth revisiting only if a future request's design output regularly approaches the ceiling.
- **Design is the first stage to exercise the full `create_branch()` → `commit_files()` → `open_pr()` chain.** Confirmed working end-to-end against the real repo: `design/REQ-2026-01` branch created from `main`, all three files committed in one commit, draft PR opened (`forge-demo-apps` PR #4) with a cross-link back to the FORGE tracking issue, summary comment posted to `forge-template#2` with the agent-comment marker.
- **Observed but not fixed (minor, same category as the existing Document 6 label-reference gap):** `open_pr()` opens all agent PRs as draft (ADR-0009's "PRs are opened by the agent, confirmed by a human" framing) — meaning the Technical Approver must click "Ready for review" before GitHub allows merging. Document 6's Gate 2 text ("Merge the PR to approve") doesn't mention this extra click, and neither does Gate 3's equivalent text for the Implementation Coordinator's PR. Not fixed this chat — folds into the same future Document 6 pass that needs to add `design-approved`/`clarification-pending` to the Label Reference table.
- **Design Agent's tracking-issue comment tells the approver to apply `design-approved` after merging**, even though Document 6's own Gate 2 prose doesn't mention a label — because that label is the actual Build Plan step 4.4 trigger for Stage 3, and the comment needs to reflect what will actually drive the pipeline once Phase 4 wiring exists, not the currently-incomplete Document 6 text.

### Repository model — two repos, not one
- **Existing platform reality:** LAA's custom applications live in a single existing **monorepo** (all microservices, one repo). A separate, existing **Dynamics 365 repo** holds D365 development — out of scope for FORGE v1, flagged as a real future goal.
- **FORGE is a separate repo from the code it acts on.** FORGE's own template repo (workflows, agents, core/team config, the per-request tracking issue) never holds application source. It orchestrates *into* the custom-apps monorepo by opening branches/commits/PRs there.
- **Build/demo phase stand-in:** since there is no existing LAA monorepo available during the personal-account build phase, a new repo **`forge-demo-apps`** (private, under the `Flamespiker` GitHub account) was created in Phase 2 to play this role for App 1, App 2, and the enhancement demo. In a real team rollout, this would be the team's actual existing applications monorepo instead.
- **Cross-repo mechanics:** FORGE workflows authenticate into the monorepo via a **GitHub App installation** (scoped, revocable, attributable to "FORGE" rather than a personal token) with permissions limited to branch/commit/PR/comment/check-run operations. Triggering webhooks come from the monorepo; the tracking issue and orchestration state live in the FORGE repo.
- **Structural repo layout:**
  - FORGE repo: `.github/workflows/`, `core/` (agents, schemas — locked), `team/` (personas, config — customizable), `tracking/`
  - Monorepo: `services/<n>/` per microservice (new folder = greenfield, existing folder = enhancement), `docs/<request-id>/` for requirements.md/design.md/tasks.md
- **Branching (in the monorepo):** `main` (always deployable) ← `feature/<request-id>` ← `design/<request-id>`. FORGE's GitHub App creates/pushes these branches on the agents' behalf.
- **Traceability now crosses a repo boundary** (FORGE repo ↔ monorepo) partway through the chain. Full traceability depends on both sides writing the reciprocal link.

### Agent topology
- Ten agents total. All other than Stage 3 are stateless single-purpose calls invoked within GitHub Actions jobs, none persistent.
- **Stage 3 (Implementation):** One Managed Agents coordinator agent + three specialist subagents (Backend, Frontend, Test Writer). Coordinator runs subagents in parallel on a shared sandbox filesystem. Integration checking is native to the coordinator. (ADR-0010)
- All other stages: Codebase Ingestion, Intake, Requirements, Design, QA, Security, Deploy — stateless single-purpose calls via the base `anthropic` client / Messages API (ADR-0011, chat 21 — originally spec'd as Claude Agent SDK calls; changed after Phase 3.1 testing showed the SDK's CLI-subprocess overhead was unjustified since none of these stages use its autonomous tool-calling).

### ADO integration
- ADO Epics/Features/User Stories are created only on `requirements-approved` — never speculatively.
- Two-way link maintained between ADO items and the FORGE tracking issue.
- Exact field mapping resolved in Document 3.

### Container & deployment
- Docker images tagged by request ID + commit SHA. Azure Container Apps: `staging` (auto-deploy, no gate) → `production` (GitHub Environment, required-reviewer gate — the one irreversible action, no post-hoc review). Rollback = redeploy prior image tag.

---

## Code-Level Stack Decisions

### What FORGE mandates (core layer — agents need this to produce runnable code)
- **TypeScript** — mandated for all React/Next.js frontend code; baked into Frontend Agent and Test Writer Agent prompts
- **Jest** — mandated test framework for frontend; Test Writer Agent writes Jest tests
- **xUnit** — mandated test framework for .NET backend
- Linting runs as a CI check (core — teams cannot disable); specific ruleset is team-layer configurable

### What is decided at design time by humans (not FORGE's concern)
- CSS approach, component libraries, .NET ORM, state management, logging libraries
- Any stylistic or architectural preference that doesn't affect CI or agent code generation
- These decisions are captured in `design.md` per project and approved by the Technical Approver at the design gate

---

## Tool Decisions (Document 3)

### Security tooling (defaults set)
- **SAST:** Semgrep Community (open source, free)
- **Secrets detection:** Gitleaks (open source, MIT)
- **Dependency vulnerability scanning:** OWASP Dependency-Check (open source, Apache 2.0)
- A Critical finding from any tool sets a failing check run, blocking merge automatically

### Anthropic API billing (updated for Managed Agents)
- **Standard API (all stages except Stage 3):** Per-token, Sonnet tier
- **Managed Agents (Stage 3 coordinator + subagents):** Standard token rates **plus $0.08 per agent session-hour** of active runtime
- Per-pipeline cost estimate: ~$0.50–3 USD for token costs (Sonnet tier, revised down from $1–5 per ADR-0011's SDK-overhead removal) + $0.08–0.32 for Managed Agents runtime (estimate 1–4 hours per implementation run). Track actuals during App 1.

### ADO field mapping (resolved)
- **FORGE writes automatically:** Title, Description, Acceptance Criteria (User Stories), Parent links, State (Active), Area Path (from team config default), Tags (`forge-managed`, `<request-id>`), traceability links
- **Left to the team:** Story Points, Priority, Iteration Path, Effort, Business Value, custom org fields

### Azure Container Apps environment specifics (resolved)
- Two separate environments: `forge-staging` and `forge-production`
- Staging: scale to zero, max 2 replicas, 0.25 vCPU / 0.5 Gi
- Production: min 1 replica, max 5, 0.5 vCPU / 1.0 Gi
- Rollback = redeploy prior image tag; previous revision retained 48h
- **Build-phase note:** in the current Azure portal, a Container Apps environment can only be created as a byproduct of creating an actual Container App (not as a standalone resource). Both `forge-staging` and `forge-production` were provisioned in Phase 2 using Azure's built-in "quickstart image" as a disposable placeholder app, then deleting just the placeholder app afterward while keeping the environment.

### GitHub App permission scoping (resolved)
- App named `forge-pipeline` (App ID `4388813`); installed on `forge-demo-apps` only, not org-wide
- Permissions: Contents (R/W), Pull requests (R/W), Issues (R/W), Checks (R/W), Metadata (R)
- Credentials stored as repo-level secrets: `FORGE_APP_ID` and `FORGE_APP_PRIVATE_KEY`; Client ID stored as repo-level **variable** `FORGE_APP_CLIENT_ID` (not a secret — Client ID is publicly visible on the App's settings page)
- Short-lived installation token generated per job via `actions/create-github-app-token@v3` (bumped from the deprecated Node-20-only `@v1` during Phase 2; `@v3` also renamed the `app-id` input to `client-id`)

### Cost summary (updated)
- Build phase incremental cost: Anthropic API tokens (~$0.50–3 USD per full pipeline run for token costs, revised down from $1–5 per ADR-0011's SDK-overhead removal, plus ~$0.08–0.32 for Managed Agents runtime — track actuals) + Azure Container Registry (~$0.17/day)
- No net-new SaaS contracts required with default tool choices
- ACR has no pause/deallocate option — only delete/recreate stops the daily charge. Accepted as a small ongoing build-phase cost rather than torn down between work sessions.

---

## Document List (in production order)

| # | Document | Status | Managed Agents Update Required |
|---|----------|--------|-------------------------------|
| 0 | FORGE Introduction | ✅ Complete | No |
| 1 | FORGE Product Specification | ✅ Complete — **updated (chat 18, `01-forge-product-specification_v2.md`)** | Yes — Section 3.3 Implementation Coordinator correction (originally mis-scoped as "No" — see chat 18) |
| 2 | FORGE Architecture Document | ✅ Complete — **updated (chat 12, `02-forge-architecture-document-v2.md`)** | Done |
| 3 | Tool & Licensing Inventory | ✅ Complete — **updated for ADR-0011 cost-estimate alignment (chat 24, `03_FORGE_Tooling_v7.md`)** | Done (Managed Agents, chat 14); Done (ADR-0011, chat 24) |
| 4 | FORGE Governance Model | ✅ Complete — **needs update** | Yes — ADR-0010 added to seed ADR list |
| 5 | AI Foundations Guide | ✅ Complete — **updated (chat 15, `05_AI_Foundation_v2.md`)** | Done |
| 6 | Orchestration Manager Guide | ✅ Complete — **updated (chat 16, `06_Orchestration_v2.md`)** — ⚠️ **new gap found chat 26:** Label Reference table is missing `clarification-pending` and `design-approved`, both real trigger/state labels per the Build Plan. Not yet fixed — needs its own dedicated doc-update chat. | Done |
| 7 | Customization Reference | ✅ Complete — **updated (chat 16, `07_Customization_Ref_v2.md`)** | Done |
| 8 | Excel Intake Template | ✅ Complete | No |
| 9 | FORGE README | ✅ Complete — **corrected via Claude Code verification (chat 25, `09-forge-readme_v6.md`)** | Done (ADR-0010, chat 17); Done (ADR-0011, chat 23–24); Done (real errors found + fixed, chat 25) |
| — | FORGE Build Plan | ✅ Complete — **updated for ADR-0011 (chat 25, `FORGE_Build_Plan_v4.md`)** — ⚠️ **new gap found chat 26:** no step anywhere (Phase 1 or Phase 4) actually creates the nine GitHub labels the pipeline depends on. All nine were created manually via `gh label create` this chat as a one-off; a real Build Plan step is still needed (Phase 8.4's fresh-clone verification would otherwise hit this). Not yet fixed. | Done (chat 13); Done (ADR-0011, chat 25) |

**Update priority order for new chats:**
1. ~~Document 2 (Architecture) — highest priority, other docs reference it~~ — done, chat 12
2. ~~Document 4 (Governance) — seed ADR list needs ADR-0010~~ — **done** (see chat 22 drift-correction note; incorrectly tracked as outstanding for six sessions)
3. ~~Build Plan — build sequence changes before Phase 3~~ — done, chat 13
4. ~~Document 3 (Tool Inventory) — billing update~~ — done, chat 14
5. ~~Document 5 (AI Foundations) — training materials + orchestration concepts~~ — done, chat 15
6. ~~Document 6 (Orchestration Manager Guide) — failure handling~~ — done, chat 16
7. ~~Document 7 (Customization Reference) — Agent Configuration + Pipeline & Orchestration sections~~ — done, chat 16 (turned out to be more than "minor" — see session note)
8. ~~Document 9 (README) — minor update~~ — done, chat 17 (ADR-0010); done again, chat 23 (ADR-0011)
9. ~~Document 4 (Governance) — ADR-0010 seed ADR list~~ — **done**, not actually outstanding (see chat 22 drift-correction note)

**ADR-0011 update queue (post chat-22 code verification):** ~~Document 2~~ → ~~Document 3 (v6, further aligned to v7 chat 24)~~ → ~~Document 9 (v4 → v5 → v6, chat 23–25)~~ → ~~Build Plan (v4, chat 25)~~ → ~~Document 6 (try/except requirement)~~ — **queue fully closed as of chat 25.**

---

## Pipeline Stages (summary)

| Stage | Name | Trigger | Output | Human Gate | Agent Type |
|-------|------|---------|--------|------------|------------|
| 0a | Codebase Ingestion *(enhancement only)* | Enhancement flagged | Existing architecture summary (monorepo, `docs/<request-id>/`) | No | Messages API call |
| 0b | Intake & Clarification | BA uploads spreadsheet | Answered context questions (FORGE tracking issue) | No | Messages API call |
| 1 | Requirements | Clarification complete | Draft ADO work items + requirements.md (monorepo) | ✅ Approve ADO items | Messages API call |
| 2 | Spec & Design | Requirements approved | design.md, openapi.yaml, tasks.md (monorepo, `design/<request-id>` branch/PR) | ✅ Approve design | Messages API call |
| 3 | Implementation | Design approved | Feature branch (backend + frontend + tests, via Managed Agents coordinator, monorepo) | ✅ Review draft PR | **Managed Agents coordinator + 3 subagents** |
| 4 | QA | Implementation PR opened | Test report, bug tickets, loop-back if failures | ✅ QA sign-off | Messages API call |
| 5 | Security | Implementation PR opened (parallel with QA) | Severity-tagged PR comments, blocks on Critical | ✅ Security sign-off | Messages API call |
| 6 | Deploy | All gates pass | Deployed to Azure Container Apps (staging → prod) | ✅ One-click approval | Messages API call |

*(Terminology corrected chat 27 — this table still said "SDK call" for every non-Stage-3 row, stale since ADR-0011 switched those six stages to the base `anthropic` Messages API client. Caught while updating this document for Design Agent completion; no other content in the table changed.)*

---

## Agent Roster

- **Intake Agent** — reads BA spreadsheet, asks clarifying questions (Messages API call) — ✅ built, `intake_agent.py`
- **Requirements Agent** — produces requirements.md and draft ADO work items (Messages API call) — ✅ built, `requirements_agent.py`
- **Design Agent** — produces design.md, openapi.yaml, tasks.md (Messages API call) — ✅ built, `design_agent.py`
- **Implementation Coordinator** — Managed Agents coordinator; orchestrates Backend, Frontend, Test Writer subagents in parallel on shared sandbox filesystem; commits complete implementation, opens draft PR (Managed Agents) — ✅ built, `implementation_coordinator.py`; real run verified, PR #5 merged
- **Backend Agent** — implements .NET API per spec (Managed Agents subagent, parallel) — ✅ built, `subagents/backend_agent.py`
- **Frontend Agent** — implements React/Next.js UI (Managed Agents subagent, parallel) — ✅ built, `subagents/frontend_agent.py`
- **Test Writer Agent** — writes unit and integration tests (Managed Agents subagent, parallel) — ✅ built, `subagents/test_writer_agent.py`
- **QA Agent** — runs tests, files bugs, loop-back to implementation (Messages API call) — ✅ built, `qa_agent.py`; unit-tested with synthetic data, no live run yet
- **Security Agent** — SAST, OWASP, secrets detection, parallel with QA (Messages API call)
- **Deploy Agent** — containerizes, pushes to Azure Container Apps (Messages API call) — ✅ built, `deploy_agent.py` (merged `forge-template#3`); real run verified for `DocumentApi` and `EmailWorker` against `REQ-2026-01` — `DocumentApi` live and responding in `forge-staging`; `EmailWorker` deployed but crash-looping (no secrets-wiring mechanism yet, see Open Questions); frontend unit not yet verified (blocked on an unrelated app dependency bug, see Open Questions)
- **Codebase Ingestion Agent** — reads existing repo for enhancement workflow (Messages API call)

---

## Open Questions / To Be Decided

- Whether orchestration managers get a shared skills library to contribute to
- Whether to enforce reciprocal traceability links (FORGE tracking issue ↔ monorepo PR) with a hard workflow failure, or handle it as guidance in the Orchestration Manager Guide
- Future Dynamics 365 support for FORGE — not designed yet, would need its own agent topology
- GitHub Actions minutes actual consumption per pipeline run — measure during App 1 build phase
- **Anthropic API per-run cost actuals (token + Managed Agents session-hour)** — measure during App 1 and App 2 runs; update Document 3 cost summary
- Docker Desktop licensing for local developer use — verify LAA non-profit eligibility or standardize on Rancher Desktop
- ADO service principal vs. PAT — PAT acceptable for build phase; evaluate service principal before production cutover
- **Managed Agents beta stability** — monitor for breaking changes to `managed-agents-2026-04-01` header behaviour during build phase; flag any disruptions as candidates for RFC
- **Managed Agents archive race condition** (see build-phase notes under ADR-0010 above) — build retry-with-backoff into the Phase 3 wrapper (step 3.1) rather than treating a transient archive failure as a hard error
- **No process exists for a requirement/User Story changing after ADO items already exist** (found chat 26). Document 6's recovery guidance only covers "the agent produced something substantially wrong" (close the tracking issue, open a new request) — not "requirements legitimately evolved after `requirements-approved` was already applied and ADO items already created." Needs a decision in a future Document 6 or Governance session: informal ADO-edit-and-move-on (normal backlog management) vs. a formal change-request path back through the pipeline. Deliberately not resolved this chat.
- **Phase 4 design note for step 4.3 (`02-design.yml`, ADO item creation):** recommend failing the whole workflow run closed if *any* ADO item fails to create, so the Design Agent can never start against a partial/incomplete traceability chain. Not yet implemented — Phase 4 hasn't started — but worth carrying forward so it isn't lost by the time that step is actually built.
- **Document 6's Label Reference table and the Build Plan's label-creation step are both real, confirmed gaps** (see Document List table above) — need their own dedicated update chats, not fixed this session.
- **Deploy Agent has no mechanism for wiring app secrets/connection strings into a deployed Container App** (found chat 34, real run). `EmailWorker` deploys successfully but crash-loops in `forge-staging` — its `ServiceBusClient` fails at startup with no connection string configured, and nothing in the current design (Deploy Agent or otherwise) sets one. Needs its own design decision before `EmailWorker` can actually run in staging — likely something like Deploy Agent reading a `secrets`/`connection-strings` block from `team/config.yaml` or Key Vault and wiring it via `az containerapp secret set` + env var references at create/update time. Not started; explicitly out of scope for chat 34.
- **Frontend unit never verified against a real Deploy Agent run** (chat 34). `npm run build` — the first time any stage has actually run a full Next.js build/type-check, since QA's `npm test` only exercises Jest — surfaced two real, previously-undiscovered `forge-demo-apps` bugs: (1) `Navigation.tsx`'s custom icon prop type was narrower than `lucide-react`'s actual `aria-hidden` typing — **fixed and merged** (`forge-demo-apps#8`); (2) a duplicate-dependency issue (`@microsoft/applicationinsights-core-js` resolved at two different `node_modules` paths, producing two structurally-identical-but-nominally-distinct types) in `lib/app-insights.ts:70` — **not yet investigated**, likely needs a `package.json` `overrides`/`resolutions` pin or a dependency-tree cleanup. Frontend containerization/deploy is blocked until this is resolved.

---

## Recommended Pre-Build Reading

Before Phase 3 (Agent Implementation), every developer working on the agent layer should read or complete:

| Resource | Priority | Why |
|----------|----------|-----|
| [Anthropic Academy](https://anthropic.skilljar.com) | **Before Phase 3** | Claude API, Agent SDK, MCP — 4–6 hours on the relevant modules |
| [Managed Agents Quickstart](https://platform.claude.com/docs/en/managed-agents/quickstart) | **Before Phase 3** | How to define agents, environments, and agent sessions — directly used in Stage 3 |
| [Managed Agents Multi-agent docs](https://platform.claude.com/docs/en/managed-agents/multi-agent) | **Before Phase 3** | The coordinator/subagent pattern FORGE's Stage 3 is built on |
| [Claude Cookbooks — managed_agents folder](https://github.com/anthropics/claude-cookbooks/tree/main/managed_agents) | **Before Phase 3** | Coordinator running three specialists in parallel — near-exact pattern of Backend/Frontend/Test Writer |
| [Claude Agent SDK docs](https://docs.anthropic.com/en/docs/claude-code/sdk) | **Before Phase 3** | All stages except Stage 3 use this directly |
| [Tool Use overview](https://docs.anthropic.com/en/docs/agents-and-tools/tool-use/overview) | Before Phase 3 | How tool calling works — every agent uses tools |
| GitHub Actions cross-repo auth with GitHub Apps | Before Phase 2 | `actions/create-github-app-token` pattern — ✅ used in Phase 2, note the `@v3`/`client-id` update above |
| Azure Container Apps quickstart (Microsoft Learn) | Before Phase 2 | Infrastructure provisioning — ✅ done in Phase 2 |
| CCA-F certification | After App 1 | Better understood after real agent work |

---

## Session Notes

- **2026-07-21:** Initial planning session. Full pipeline, tech stack, document list, and platform model established.
- **2026-07-21 (chat 2):** Document 0 — FORGE Introduction — drafted and completed.
- **2026-07-21 (chat 3):** Document 1 — FORGE Product Specification — drafted and completed.
- **2026-07-21 (chat 4):** Document 2 — FORGE Architecture Document — drafted and completed. Major structural correction mid-session: two-repo model established (FORGE repo + existing custom-apps monorepo).
- **2026-07-21 (chat 5, addendum):** Code-level stack decisions finalized. TypeScript/Jest/xUnit mandated at core layer.
- **2026-07-21 (chat 5):** Document 3 — Tool & Licensing Inventory — drafted and completed.
- **2026-07-21 (chat 6):** Document 4 — FORGE Governance Model — drafted and completed. RFC process, ADR format, nine seed ADRs identified.
- **2026-07-21 (chat 7):** Document 5 — AI Foundations Guide — drafted and completed. Nine sections including client data sensitivity callout and external training list.
- **2026-07-21 (chat 8):** Document 6 — Orchestration Manager Guide — drafted and completed. Five parts: setup, running, customization, failure handling, production checklist.
- **2026-07-21 (chat 9):** Document 7 — Customization Reference — drafted and completed. ~65 items explicitly categorized Locked/Flexible/Fully Open.
- **2026-07-21 (chat 10):** Document 8 — Excel Intake Template — created as formatted .xlsx workbook.
- **2026-07-22 (chat 11):** Pre-build architectural review. Two decisions made:
  1. **Rejected CrewAI** as the multi-agent framework — conflicts with GitHub Actions as orchestrator, stateless-per-stage design, and human gate architecture. No third-party agent framework adopted.
  2. **Adopted Anthropic Managed Agents for Stage 3** (ADR-0010) — Implementation Coordinator agent runs Backend, Frontend, Test Writer subagents in parallel on a shared sandbox filesystem via Managed Agents multi-agent orchestration. All other stages remain standalone Claude Agent SDK calls. Billing: $0.08/agent session-hour active runtime in addition to token costs.
  - **Terminology note established:** "chat/chat thread" = project writing sessions; "agent session" = Managed Agents API runtime instance.
  - **Training materials:** Claude Cookbooks (managed_agents folder) and Managed Agents docs added to pre-build reading list; Document 5 needs update.
  - **Nine documents identified** as needing Managed Agents updates before build begins. Update priority order documented in Document List section above.
  - Next: Update Document 2 (Architecture) in a new chat, then proceed in priority order before starting Phase 1 of the Build Plan.
- **2026-07-23 (chat 12):** Document 2 (Architecture) updated per the doc2-change-brief — produced `02-forge-architecture-document-v2.md`. Changes applied: Section 2.2 (Managed Agents exception + billing note), ADR-0002 clarification paragraph (in Section 1, where the stateless-invocation text actually lives), Section 3 agent topology table (Implementation Coordinator + 3 subagent rows replacing the old 3-parallel-job rows, plus updated note paragraph), Section 4.5 mechanics rewrite + billing line, Section 9 traceability chain parenthetical, Section 10 new open item (Managed Agents beta stability), new Section 11 (ADR-0010 Reference), "Where This Fits" renumbered to Section 12. No other content changed. File is in `/mnt/user-data/outputs/` awaiting upload to the project to replace `02-forge-architecture-document.md`.
  - Next: Document 4 (Governance) — add ADR-0010 to the seed ADR list — per document list priority order.
- **2026-07-23 (chat 13):** Build Plan updated for ADR-0010 out of priority order (Document 4 still pending — user chose to do the Build Plan next; revisit Document 4 next). Produced `FORGE_Build_Plan_v2.md`, intended to replace `FORGE_Build_Plan.md` in the project. Changes applied: Phase 2 new step 2.9 (Managed Agents access check — beta header, throwaway test session); Phase 3 step 3.1 adds a Managed Agents API wrapper alongside the existing SDK wrapper; new step 3.4a (Implementation Coordinator agent); steps 3.5–3.7 (Backend/Frontend/Test Writer) reframed as Managed Agents subagent definitions rather than independently invoked/committing scripts; Phase 4 step 4.4 rewritten (coordinator session invocation replaces three parallel jobs, no separate integration-check job); step 4.8 branch protection updated (integration-check removed from required status checks); step 4.9/4.10 language updated for coordinator-authored links and Console audit trail. Phase 5 step 5.6 and 5.10 updated for coordinator review language and Managed Agents session-hour cost tracking. Phase 6/7/8 minor references updated (seed ADR count 9→10, Phase 8.4 includes Managed Agents access verification). No other phases changed.
  - File is in `/mnt/user-data/outputs/` awaiting upload to the project — the user will upload it and remove the old `FORGE_Build_Plan.md`.
  - Next: Document 4 (Governance) — add ADR-0010 to the seed ADR list — per document list priority order (still outstanding).
- **2026-07-23 (chat 14):** Document 3 (Tool & Licensing Inventory) updated for ADR-0010 out of priority order (Document 4 still pending — user chose Document 3 next; revisit Document 4 next). Produced `03_Tooling_v2.md`, intended to replace `03_Tooling.md` in the project. Changes applied: §3.3 split the Anthropic API row into Anthropic API (account/billing umbrella), Claude Agent SDK (rescoped to "all stages except Stage 3"), new Managed Agents row (coordinator/subagent runtime, beta header, $0.08/session-hour billing), and renamed model row to "Claude model tiers" (Opus for coordinator, Sonnet elsewhere); cost estimation note updated with the $0.08–0.32/run Managed Agents estimate; §8 Cost Summary table gained a dedicated Managed Agents runtime row and reworded totals; §9 Open Items merged the Anthropic cost-actuals item to cover both token and session-hour costs and added a new Managed Agents beta stability monitoring item. No other sections changed.
  - File is in `/mnt/user-data/outputs/` awaiting upload to the project — the user will upload it and remove the old `03_Tooling.md`.
  - Next: Document 4 (Governance) — add ADR-0010 to the seed ADR list — per document list priority order (still outstanding, now the last document remaining from the original priority list before moving to Documents 5–7 and 9).
- **2026-07-23 (chat 15):** Document 5 (AI Foundations Guide) updated for ADR-0010 out of priority order (Document 4 still pending — user chose Document 5 next; revisit Document 4 next). Produced `05_AI_Foundation_v2.md`, intended to replace `05_AI_Foundation.md` in the project. Changes applied: new Terminology Note section (chat/chat thread vs. agent session, mirroring this context doc); Section 3 new "Managed Agents exception (Stage 3)" callout explaining the coordinator/subagent pattern and why it doesn't violate stateless-per-stage, plus two new reference links; Section 9 gained three new entries after Anthropic Academy and before CCA-F — Managed Agents Quickstart, Managed Agents Multi-agent Patterns, Claude Cookbooks (managed_agents folder). Version 1.0 → 1.1, date updated. No other sections changed.
  - File is in `/mnt/user-data/outputs/` awaiting upload to the project — the user will upload it and remove the old `05_AI_Foundation.md`.
  - Next: Document 4 (Governance) — add ADR-0010 to the seed ADR list — per document list priority order (still outstanding — now the only document remaining from the original priority list before Documents 6, 7, and 9).
- **2026-07-23 (chat 16):** Documents 6 (Orchestration Manager Guide) and 7 (Customization Reference) reviewed against ADR-0010 and updated in the same chat (user opted out of the usual one-chat-per-document rule for this session). Produced `06_Orchestration_v2.md` and `07_Customization_Ref_v2.md`, intended to replace `06_Orchestration.md` and `07_Customization_Ref.md` in the project.
  - **Document 6 changes:** Part 2, Gate 3 description rewritten — now describes the Implementation Coordinator running Backend/Frontend/Test Writer subagents on a shared sandbox filesystem rather than three independent jobs. Part 4 Agent Failures gained a new entry ("The Implementation Coordinator session failed (Stage 3)") plus a clarifying note that the existing silent-failure entry applies to non-Stage-3 stages only. Part 4 Infrastructure Failures gained a new entry for Managed Agents beta-header/access issues. Reference → File Reference table updated to attribute `services/<service-name>/` to Backend/Frontend subagents via the coordinator. No other sections changed.
  - **Document 7 changes:** review surfaced more drift than the "minor clarification" flagged in the prior session note — several rows stated things that ADR-0010 had actually made incorrect, not just outdated. Pipeline & Orchestration section: agent invocation model row gained the ADR-0002/Stage-3 exception note. Agent Configuration section: agent roster row corrected from ten to eleven agents (added Implementation Coordinator); agent execution model row gained the Managed Agents exception for Stage 3; parallel execution row reworded around subagents/coordinator; **the "Integration check job... cannot be removed or bypassed" row was factually wrong post-ADR-0010 and was replaced** with an "Integration handling in Stage 3" row describing the coordinator's native integration handling; agent model tier row corrected from "Sonnet (all agents)" to the Opus-for-coordinator/Sonnet-elsewhere split. Quick-Reference Summary counts checked and left unchanged — no row moved between Locked/Flexible/Fully Open categories, so the approximate counts still hold.
  - Next: Document 4 (Governance) — add ADR-0010 to the seed ADR list — still the only document remaining from the original ADR-0010 priority list. Document 9 (README) also still outstanding (minor note on Stage 3).
- **2026-07-23 (chat 17):** Document 9 (README) reviewed and updated for ADR-0010. Review surfaced three factual corrections, not just the "minor note" flagged in prior session notes. Produced `09-forge-readme_v2.md`, intended to replace `09-forge-readme.md` in the project.
  - **Changes applied:** Pipeline diagram Implementation-stage line rewritten from "backend + frontend + tests run in parallel" to "Implementation Coordinator runs Backend, Frontend, Test Writer subagents in parallel." Prerequisites section's Anthropic API line updated to note Managed Agents beta access (`managed-agents-2026-04-01` header) and the Opus-for-coordinator/Sonnet-elsewhere tier split (previously just said "Sonnet tier recommended"). Cost reference table gained a dedicated Managed Agents runtime row (~$0.08–0.32 USD, session-hour billing) and relabeled the Anthropic API row as token costs specifically. Repository layout's `core/agents/` line updated to note it includes the coordinator + subagent definitions. No other sections changed — Approving-a-gate table and reference documentation links were checked and found still accurate.
  - File is in `/mnt/user-data/outputs/` awaiting upload to the project — the user will upload it and remove the old `09-forge-readme.md`.
  - **Document 4 (Governance) is now the only document remaining from the original ADR-0010 update list** — outstanding since chat 12, deferred in favour of other documents three times.
- **2026-07-23 (chat 18):** User asked whether Documents 0 and 1 needed ADR-0010 updates. Document 0 (Introduction) confirmed accurate as-is — its Implementation-stage line ("implements the backend, frontend, and tests in parallel") stays true at its level of abstraction and doesn't claim the old three-independent-jobs model. **Document 1 (Product Specification) was found to need a correction**, despite being marked "No" in the document list — Section 3.3 omitted the Implementation Coordinator entirely and incorrectly attributed the feature-branch commit and draft-PR-open action to the three subagents rather than the Coordinator. Produced `01-forge-product-specification_v2.md`, intended to replace `01-forge-product-specification.md` in the project.
  - **Change applied:** Section 3.3 rewritten — the Implementation Coordinator now explicitly orchestrates Backend/Frontend/Test Writer in parallel on a shared sandbox, synthesizes their output, and is the one that commits the implementation and opens the draft PR. No other sections changed.
  - File is in `/mnt/user-data/outputs/` awaiting upload to the project — the user will upload it and remove the old `01-forge-product-specification.md`.
  - **Document 4 (Governance) remains the only document from the original ADR-0010 priority list still outstanding** — now deferred four times in favour of other work.
- **2026-07-23 (chat 19):** Phase 1 build complete. `forge-template` GitHub repository created (template flag enabled, public). All 10 Phase 1 steps completed and pushed.
  - **Steps completed:** 1.1 repo created on GitHub; 1.2 full folder structure with `.gitkeep` files; 1.3 `team/config.yaml` with Container Apps values from architecture decisions; 1.4 `team/stack-preferences.yaml` with core-layer mandates vs team-layer placeholders clearly distinguished; 1.5 GitHub issue form template (`forge-request.yml`) with all pipeline stage checkboxes; 1.6 `.gitignore` (Python + Node + .NET + FORGE-specific) and `.gitattributes` (LF line endings enforced); 1.7 README.md populated from Document 9; 1.8 10 seed ADR stubs in `core/decisions/`; 1.9 7 GitHub Actions workflow stubs in `.github/workflows/`; 1.10 repo verified clean on GitHub.
  - **Additional:** `/docs` folder added to the repo containing all FORGE documents (all v2 files). Duplicate of Document 9 removed from `/docs` after being promoted to `README.md`.
  - **Two-machine workflow established:** Code stays in local folders on each machine (not OneDrive). Git push/pull via GitHub is the sync mechanism between machines.
  - **Build session structure agreed:** Roughly one phase per chat thread. Start a new chat at phase boundaries or when context feels stale. Paste the phase and last completed step at the start of each new build chat.
  - **Learning approach:** Just-in-time — concepts introduced immediately before the phase that needs them. Claude Code CLI handles file writing; this chat handles strategy, explanation, and review.
  - **Document 4 (Governance) — ADR-0010** is the only outstanding document update. Handle in a dedicated doc-update chat before or during Phase 2 — not blocking Phase 2 infrastructure work but should be done before Phase 3.
  - Next: Phase 2 — Infrastructure Setup. Start a fresh chat.
- **2026-07-25 (chat 20 — this chat):** Phase 2 (Infrastructure Setup) complete — all nine steps (2.1–2.9) verified working end-to-end.
  - **2.1 GitHub App:** Created `forge-pipeline` (App ID `4388813`), installed on `forge-demo-apps` only (not org-wide). Permissions: Contents R/W, Pull requests R/W, Issues R/W, Checks R/W, Metadata R. Credentials stored as `FORGE_APP_ID`, `FORGE_APP_PRIVATE_KEY` secrets, and `FORGE_APP_CLIENT_ID` as a repo **variable** in `forge-template` (Client ID needed because the current `create-github-app-token@v3` action deprecates the old `app-id` input in favour of `client-id`).
  - **New repo created:** `forge-demo-apps` (private, under personal GitHub account **`Flamespiker`**) — stands in for LAA's real application monorepo during the build/demo phase, since no such repo exists yet in this personal-account context. This is the repo FORGE actually builds into; distinct from `forge-template`, which holds only FORGE's own orchestration machinery.
  - **2.2 Azure Container Registry:** Created (Basic tier), admin-user credentials stored as `ACR_LOGIN_SERVER`, `ACR_USERNAME`, `ACR_PASSWORD` secrets. Confirmed ACR has no pause/deallocate option — only delete/recreate stops the daily charge — accepted as a small ongoing build-phase cost rather than torn down between sessions.
  - **2.3 / 2.4 Container Apps environments:** `forge-staging` and `forge-production` environments created (Consumption plan). Portal note: environments can currently only be created as a byproduct of creating a Container App — used Azure's "quickstart image" placeholder trick to provision each environment, then deleted the placeholder container app afterward, keeping the environment itself.
  - **2.5 GitHub Environments:** `staging` (no required reviewers, auto-deploy) and `production` (required reviewer: Mike) configured in `forge-template`.
  - **2.6 ADO connection:** New ADO project **`FORGE-Build`** created under org **`https://dev.azure.com/spike99`** (Agile process) to act as the one tracking project for all build-phase apps (App 1, App 2, enhancement all show up as separate Epics inside it, mirroring the `services/<n>/` folder split in the monorepo). PAT generated (scoped to `spike99` only, not all orgs), verified via a real test Epic created through the ADO REST API and then deleted, stored as `ADO_PAT` secret.
  - **2.7 Anthropic API key:** Personal developer key confirmed active, stored as `ANTHROPIC_API_KEY` secret.
  - **2.8 End-to-end connectivity check:** Built `.github/workflows/verify-setup.yml` (permanent — reused in Phase 8.4 to verify a fresh clone), verifying GitHub App token generation + repo access, ADO connectivity, and Anthropic API access in one workflow. Notable debugging along the way, worth remembering for Phase 3: `actions/create-github-app-token` needed bumping from the deprecated `@v1` (Node 20, being forced onto Node 24 with warnings) to `@v3` (Node 24-native, and renames `app-id` to `client-id`); several secrets/variables assumed stored earlier in the session (`FORGE_APP_ID`, `FORGE_APP_PRIVATE_KEY`, `FORGE_APP_CLIENT_ID`) turned out not to have actually been saved and had to be added properly on a second pass; and a copy-paste mix-up briefly had a verification step's GitHub API call pointed at the ADO org name (`spike99`) instead of the actual GitHub owner (`Flamespiker`) — two similarly-shaped "org" identifiers that are easy to cross. All resolved; workflow now passes cleanly with three green checks.
  - **2.9 Managed Agents access check:** Verified via two throwaway local scripts (`managed_agents_check.py`, `archive_retry.py` — gitignored, not committed). Full lifecycle confirmed: beta header accepted, agent/environment/session creation, message send, idle state reached, and cleanup archival all worked (with one transient archive-timing hiccup, resolved by retry — see "Managed Agents API — build-phase notes" under ADR-0010 above for the exact schema/behavior details to carry into Phase 3).
  - **Process lesson for future phases:** several credential-storage steps this session were verbally confirmed "done" but had not actually been saved in GitHub's UI, causing repeated debugging loops before the real problem (a missing secret/variable) was found. Worth explicitly confirming (screenshot or listing) that secrets/variables actually appear in GitHub's UI before moving to the next step, rather than treating a verbal "done" as sufficient for credential-storage steps specifically.
  - **Formatting preference established:** anything meant to be handed to Claude Code, and all git commands, should be given in fenced code blocks so the copy button is available — apply this from now on in all future chats, not just this one.
  - **Document 4 (Governance) — ADR-0010 seed ADR list is still the only outstanding document update**, now deferred five times (four document-update chats, plus the entire Phase 2 build) in favour of other work. Per the chat 19 note, this should land before Phase 3 begins.
  - Next: Phase 3 — Agent Implementation. Start a fresh chat.
- **2026-07-29 (chat 21 — this chat):** Phase 3.1 (Shared Agent Utilities) fully complete — all five modules built, reviewed, and **verified against real services with real evidence**, not just code review. User explicitly chose to proceed with Phase 3 this session rather than land Document 4 first — that item is now deferred six times (see below).
  - **`file_io.py`** — 7/7 passed. Three real bugs found and fixed during review, all confirmed against the actual `Intake_Template.xlsx` (not assumed): (1) Requirements-sheet header search was scanning every cell for the substring "req" instead of exact-matching column A — fixed to `row[0].strip().lower() == "req #"`; (2) an `_is_example_row()` heuristic meant to skip the four pre-populated example rows was flagged for removal but initially left in place on the first fix attempt — genuinely removed on the second pass, confirmed by re-running against the real file; (3) Overview-section detection used exact match against bare section names ("Request Identification") when the real cells have letter prefixes ("A — Request Identification") — fixed via substring matching, then further improved to return canonical snake_case dict keys (`request_identification`, etc.) with nested `{field_label: field_value}` dicts per section, rather than the raw spreadsheet text or a flat interleaved list. A separate portability bug (wrong `Path.parents[]` index, overshooting the repo root by one level) was also found and fixed, confirmed genuinely portable (anchored to `__file__` depth, not any absolute path).
  - **`claude_agent_wrapper.py`** — 5/5 passed, then superseded by ADR-0011 (see below). Initial build mistakenly used the base `anthropic` package instead of the real Claude Agent SDK; corrected to the actual `claude-agent-sdk` PyPI package (verified real via direct PyPI fetch — v0.2.128 confirmed current) after Claude Code initially disputed the package's existence. Cost investigation after an anomalous $0.09652-for-3-tokens smoke test result correctly diagnosed the cause as Claude Code CLI system-prompt/tool-definition cache creation (~25,700 tokens on a cold call) rather than a per-invocation minimum charge — this diagnosis directly led to ADR-0011.
  - **`ado_helper.py`** — 4/4 passed. Real bug found via live API testing: Document 3 §5 specified `"Active" on creation` for all four work item types (Epic, Feature, User Story, Bug); the live ADO API only permits creation in `"New"` — `"Active"` is transition-only for all four types, confirmed via `/wit/workitemtypes/{type}/states`. Fixed by dropping `System.State` from all four `_make_patch` calls (defaults to `"New"`). **Document 3 corrected same-session (v5)** — the four State rows changed from `"Active" on creation` to `"New" on creation`, with an explanatory note.
  - **`github_helper.py`** — 7/7 passed. Real architecture bug found: `post_comment`/`add_label`/`remove_label` were targeting `FORGE_TARGET_REPO` (the monorepo, `forge-demo-apps`) when the tracking issue actually lives in `FORGE_SOURCE_REPO` (`forge-template`) — would have silently posted tracking-issue updates to the wrong repo in production. Fixed: those three functions now use a local-dev-only `GITHUB_TOKEN` (explicitly documented in code as a stand-in for the ambient token GitHub Actions injects automatically for same-repo ops — must NOT be added as a real Actions secret in Phase 4) targeting `FORGE_SOURCE_REPO`, while `create_branch`/`commit_files`/`open_pr` correctly stay on the GitHub App's installation token targeting `FORGE_TARGET_REPO`. Also: `commit_files()` was missing entirely from the original build (needed by 3.4 and 3.4a, not just this smoke test) — added, verified via a real signed commit (`forge-pipeline[bot]`, `verified: true`) into `forge-demo-apps`.
  - **`managed_agents_wrapper.py`** — 6/6 passed on the second attempt. **Real, substantial Managed Agents API schema change discovered and confirmed against Anthropic's official docs** (`platform.claude.com/docs/en/managed-agents/*`, `github.com/anthropics/skills`, `claude-cookbooks/managed_agents`): the `"subagents"` field assumed since Phase 2.9 no longer exists (if it ever did — Phase 2.9's own verification was explicitly a zero-subagent test, so this may never have been validated rather than having regressed). Current schema: subagents must be created as independent, standalone agent resources first, then a coordinator agent references them by ID via `multiagent: {"type": "coordinator", "agents": [...]}`; environments and sessions are top-level resources (`POST /v1/environments`, `POST /v1/sessions`); errors surface as `session.error` events in the stream, not as a status value (`terminated` is a rare, unrecoverable orchestration-layer state, not the general error signal); per-subagent audit trail comes from `client.beta.sessions.threads.list(session_id)` (coordinator = primary thread, `parent_thread_id: null`; each subagent = its own thread). First smoke test attempt passed 5/5 but was **coordinator-only** (`subagent_configs=[]`) — did not actually exercise the multi-agent path; re-run with a real declared specialist agent confirmed genuine coordinator→subagent delegation, correct thread structure, per-subagent event retrieval, and full four-resource archive (coordinator + specialist agents + environment + session).
  - **ADR-0011 decided:** the Agent SDK cost/latency investigation above surfaced a broader question — none of the six non-Stage-3 stages use the Agent SDK's autonomous tool-calling, yet every invocation pays its full CLI-subprocess overhead. Decision: switch those six stages to the base `anthropic` client (Messages API), full rationale in the ADR-0011 subsection above. **Code rewrite handed to Claude Code this session but not yet confirmed complete or re-tested** — treat as decided-but-unverified until a real diff and clean smoke test re-run are reviewed in the next chat.
  - **Process pattern that kept paying off:** insisting on real executed output (diffs, actual API responses, re-run smoke tests) rather than accepting summaries caught real problems repeatedly this session — the file_io fixes, the github_helper repo-targeting bug, the ADO state assumption, and the Managed Agents coordinator-only false-pass would all likely have shipped unnoticed under a "trust the summary" approach.
  - **Document 3 updated twice this session** (v4: added §3.7 Context7 dev-tooling note, not part of FORGE's runtime tool inventory; v5: corrected the four State-on-creation rows per the ADO finding above) — v5 is the current authoritative version in the project. A third update (v6) is still needed once ADR-0011's code change is verified (see ADR-0011 subsection above).
  - **New memory edit added:** for FORGE project work, don't use organizational skills (`laa-brand`, `laa-security-review`, `freshservice-kb-article`) unless explicitly asked.
  - **Document 4 (Governance) — ADR-0010 seed ADR list remains the only outstanding *document-list* item**, now deferred six times. **New outstanding item this chat:** Documents 2, 3, and 9 need correction for ADR-0011 once its code is verified — do not action until then.
  - Next: continue Phase 3 at step 3.2 (Intake Agent) — but **first**, confirm ADR-0011's `claude_agent_wrapper.py` rewrite is genuinely complete and re-tested (real diff, real smoke test output), since 3.2 will call `invoke_agent()` and the interface changed (`allowed_tools` removed, `max_tokens` added). Starting 3.2 against an unverified wrapper interface risks building against a signature that isn't final.
  - **[Correction added chat 22 — see below]:** the "Document 4 still outstanding" claim above, and every prior instance of it back through chat 13, was stale. Document 4 was actually completed on 2026-07-23 in a session titled "FORGE 04 Governance," which produced the ADR-0010 addition and bumped that session's context doc to a v13. A **different** same-day session ("Build Plan") branched off the same earlier context version independently and became the lineage that actually continued forward into v14 → v21 — so every context doc from that point on inherited "still outstanding" from the branch that never saw the fix, even though the real, completed `04_Governance-v2.md` was uploaded to the project and has been sitting there correctly the whole time. Content was never wrong; only this document's own session log was. Confirmed by direct inspection of `04_Governance-v2.md` (ADR-0010 is present in the Seeded ADRs table) and by conversation search of the "FORGE 04 Governance" session.
- **2026-07-29 (chat 22 — this chat):** Session opened with the standard "review everything, read context first" request. Reviewed all nine project documents, `CLAUDE.md`, `requirements.txt`, and `ADR-0011.md` before making any changes, per standing process.
  - **Document 4 drift found and corrected** — see note above. No content changes needed to `04_Governance-v2.md` itself; only this context document's session-log claims were corrected (this entry, the Governance bullet under Key Decisions, and both Document List entries).
  - **ADR-0011 code status checked and confirmed still pending** — inspected `requirements.txt` (still pins `claude-agent-sdk`, does not list `anthropic`) and `CLAUDE.md` (still fully describes the pre-ADR-0011 `claude_agent_sdk` / `query()` architecture with no mention of ADR-0011). Neither file shows any trace of the rewrite landing. User confirmed verbally: still pending.
  - **Decision:** since the ADR-0011 code gate is still closed, Documents 2, 3, and 9 remain untouched this chat — updating them now would describe code that doesn't exist in the repo yet. User chose to send the ADR-0011 rewrite back to Claude Code now, rather than draft provisional doc language.
  - **Claude Code brief for the rewrite handed to the user this chat** (not yet run): rewrite `claude_agent_wrapper.py` to call the base `anthropic` Messages API directly per ADR-0011 (drop `allowed_tools`, add `max_tokens`, build a maintained per-model cost-rate table instead of reading `total_cost_usd` from an SDK `ResultMessage`); update `requirements.txt` (`anthropic` back in as a direct dependency, `claude-agent-sdk` removed); update `CLAUDE.md`'s stale Agent SDK section to match; re-run `smoke_claude_agent` for a real pass/fail with real output; produce a real diff. See the chat 22 transcript for the exact brief given.
  - **Build Plan reviewed for accuracy while user ran Claude Code.** Confirmed accurate throughout for ADR-0010 (Phases 3.1, 3.4a, 3.5–3.7, 4.4, 4.8, 4.9/4.10, 5.6, 5.10 all consistent). **Found it was missed from the ADR-0011 affected-documents list** — step 3.1's SDK-wrapper line is stale, and Phase 8.3's "ten seed ADRs" count doesn't account for ADR-0011, which also has no checklist step tracking its commit to `core/decisions/`. Added to the outstanding list above (would become Build Plan v4). Not edited this chat — same code-verification gate applies.
  - **ADR-0011 code verified this chat** — real diff, real smoke test output, independently-checked pricing table, and an honest error-handling design all reviewed and confirmed. See the verification note above. The gate is clear.
  - Next: per standing process (one document per chat thread), the ADR-0011 corrections should each get their own fresh chat, pasting in this context doc. Suggested order, following the existing priority convention (Document 2 first since other docs reference it): **Document 2 → Document 3 (v6) → Document 9 → Build Plan (v4) → Document 6 (new try/except requirement)**. Once those land, Phase 3 can continue at step 3.2 (Intake Agent) against a confirmed-final `invoke_agent()` signature.
- **2026-07-29 (chat 23 — this chat):** Document 9 (README) reviewed and updated for ADR-0011, per queue order. (Documents 2, 3, and 6 were evidently completed in intervening chats not individually logged here — found already at v3, v6, and v3 respectively in the project at the start of this chat; content spot-checked and consistent with the ADR-0011 gate-cleared plan from chat 22, so treated as done rather than re-litigated. This context doc's own session log had fallen a step behind the actual project state — same category of drift as the chat-22 Document-4 correction, just less consequential since no incorrect "still outstanding" claim was actively being repeated.)
  - **Genuine finding:** the chat-22 assumption that Document 9's "Prerequisites and cost-reference sections reference the Agent SDK" did not hold up — neither section actually named the Claude Agent SDK, so there was no factual error to correct there (unlike Documents 2 and 3, which did). Also checked whether the Prerequisites' "Node.js 20+ installed locally" line was Agent-SDK-driven — confirmed it isn't (Node.js appears elsewhere in the doc set only in connection with `create-github-app-token`'s GitHub-hosted-runner requirement and the target app's own TypeScript/Next.js stack) — left unchanged.
  - **Actual change applied:** one footnote added under the Cost reference table noting ADR-0011's consequence for readers — elimination of the ~$0.10 cold-call cost and ~10-second launch latency the Agent SDK's bundled CLI added per invocation on the six non-Stage-3 stages — worded to match Document 3's ADR-0011 note. No other section changed.
  - Produced `09-forge-readme_v4.md` (project file was already at v3, for reasons not reflected in this context doc — incremented from that actual baseline rather than the v2 the session log implied).
  - File is in `/mnt/user-data/outputs/` awaiting upload to the project — the user will upload it and remove the old `09-forge-readme_v3.md`.
  - **ADR-0011 update queue: only the Build Plan (v4) remains** — step 3.1's SDK-wrapper line, Phase 8.3's "ten seed ADRs" → eleven, and a new checklist step tracking ADR-0011's commit to `core/decisions/`.
  - Next: Build Plan (v4) in a fresh chat. Once that lands, Phase 3 can continue at step 3.2 (Intake Agent) against the confirmed-final `invoke_agent()` signature.
- **2026-07-29 (chat 24 — this chat):** User ran the chat-23 draft of Document 9 (`09-forge-readme_v4.md`) past Claude Code, which suggested two further refinements beyond what this chat had caught:
  1. **Prerequisites / Anthropic API bullet rewritten** — the prior wording ("an API key with Managed Agents beta access... for Stage 3") could read as if beta access were a blanket requirement. Reworded to state plainly that a standard key covers Intake, Requirements, Design, QA, Security, and Deploy, and that Managed Agents beta access is an *additional* requirement specific to Stage 3. Accepted without reservation — genuine clarity improvement, no downstream conflict.
  2. **Cost reference token-cost estimate revised from ~$1–5 USD to ~$0.50–3 USD**, reasoning that the lower floor reflects removal of the Agent SDK's ~$0.10 cold-call overhead per invocation across the six non-Stage-3 stages (ADR-0011). Also relabeled the Managed Agents cost row "(Stage 3 only)" for clarity.
  - **Caught before applying:** this second change would have put Document 9 out of sync with Document 3 v6, which still stated ~$1–5 for the same figure (Document 3's own ADR-0011 pass, done in an intervening untracked chat, updated the invocation-mechanism rows but never revisited the dollar estimate). Flagged this to the user rather than applying Document 9's number in isolation.
  - **User decision: align both documents on ~$0.50–3 now.** Updated Document 3 (`03_FORGE_Tooling_v6.md` → `03_FORGE_Tooling_v7.md`): the Cost Summary table row and the §3.3 cost-estimation note both revised to ~$0.50–3 USD, with an inline note explaining the figure was revised down from $1–5 to reflect ADR-0011's overhead removal. Also swept this context document itself for the same stale figure — both instances under "Anthropic API billing" and "Cost summary" (Key Decisions section) updated to match, since this document is meant to be the authoritative living reference and would otherwise itself be a third inconsistent source.
  - **Produced `09-forge-readme_v5.md`** (supersedes the chat-23 `09-forge-readme_v4.md` — that file should NOT be uploaded; use v5) and **`03_FORGE_Tooling_v7.md`**. Both are in `/mnt/user-data/outputs/`.
  - **Note for future sessions:** this is the second time in two chats that a "gate cleared, done" document turned out to need a follow-up pass (Document 3 here; the Document-9-assumption-didn't-hold finding in chat 23). Worth treating "done" status on ADR-0011 documents as provisional until Phase 3 actually exercises `invoke_agent()` against real stage scripts — a live run may well surface further drift the same way Phase 3.1 did originally.
  - **ADR-0011 update queue: only the Build Plan (v4) remains outstanding** (step 3.1's SDK-wrapper line, "ten seed ADRs" → eleven, new checklist step for ADR-0011's commit to `core/decisions/`). Document 3 is now fully current at v7 — no further ADR-0011 work expected there barring new findings.
  - Next: Build Plan (v4) in a fresh chat, pasting in this context doc.

---
- **2026-07-29 (chat 25 — this chat):** User surfaced a further-edited version of Document 9 (source unclear — not from this chat's v5) that had drifted from the actual verified build in four real ways. Since the user didn't know the answers, had Claude Code verify against the actual repo rather than reasoning from docs alone — this caught genuine errors, not just stylistic disagreements:
  1. **`FORGE_APP_ID` had been dropped from the Secrets table.** Claude Code confirmed via direct code read: `github_helper.py`'s `_build_app_jwt()` uses `os.environ["FORGE_APP_ID"]` as the JWT `iss` claim — a real, active dependency, distinct from `FORGE_APP_CLIENT_ID` (used only by the `actions/create-github-app-token` Action in `verify-setup.yml`). Both are genuinely required; neither substitutes for the other. Restored.
  2. **`AZURE_CREDENTIALS` had been added to the Secrets table as if it were a real, required secret.** Claude Code's grep of every workflow YAML found zero references — the Deploy workflow (`06-deploy.yml`) is still a guard-clause stub. More tellingly: **`06_Orchestration_v3.md` line 155 already contains an explicit warning not to assume a secret named `AZURE_CREDENTIALS` exists until the Deploy Agent's design defines it** — whoever produced the drifted README appears to have lifted that cautionary placeholder name and written it in as if it were real. Reverted to the actual currently-documented mechanism (`ACR_LOGIN_SERVER`/`ACR_USERNAME`/`ACR_PASSWORD`, per Document 6 v3's own Secrets table and Phase 2.2's verified build), with Document 6's "don't assume `AZURE_CREDENTIALS` exists yet" caveat carried over into the README too.
  3. **The GitHub Environments creation step (`staging`/`production`, Settings → Environments) had been dropped from Getting Started entirely** — a required step with no substitute; without it there's nothing for a reviewer to approve. Restored, along with the Azure-Portal placeholder-workaround note, both matching Document 6 v3 Step 5 almost verbatim.
  4. **The Approving-a-gate table said "Approve the `forge-production` GitHub Environment"** — conflating the GitHub Environment name (`production`, plain) with the Azure Container Apps environment name (`forge-production`). Document 6 v3 explicitly warns against this exact conflation. Fixed back to plain `production`.
  - **Also corrected, lower-stakes:** the Prerequisites' Anthropic API bullet had drifted to "an API key at Sonnet tier or higher" (API keys aren't tiered by model access this way) and claimed Managed Agents beta access must be "request[ed]... from Anthropic" — Claude Code confirmed Phase 2.9 observed no such approval step, just the header working directly on a standard key. Both reworded to match what was actually observed, with appropriate hedging ("may vary by account") rather than an unverified confident claim either way.
  - **What was kept from the drifted version, because it was a genuine improvement:** the cost-table restructuring (Sonnet tier for stages 0–2/4–6, Opus for the Stage 3 coordinator, folded into one "all stages" row) is more accurate than this chat's own v5 — Document 3's Anthropic API row covers token costs for all ten agents including the Stage 3 coordinator, with Managed Agents' session-hour billing being the separate additional charge, not a second token-cost bucket. Carried forward as-is.
  - **No other documents needed correction this chat** — Document 6 v3 was the source of truth throughout and was already internally correct (its `AZURE_CREDENTIALS` mention is the intentional cautionary example, not an error); Document 3 v7 doesn't reference any of these credentials either way.
  - **Process note worth keeping:** this is the same "verify against the real build, not the summary" pattern that caught the file_io/github_helper/ADO/Managed-Agents bugs back in chat 21 — worth defaulting to a Claude-Code verification pass before accepting any further hand-edited draft of a document that touches credentials, secret names, or environment identifiers.
  - Produced `09-forge-readme_v6.md`. This supersedes `09-forge-readme_v5.md` — do not upload that one. File is in `/mnt/user-data/outputs/`.
  - **ADR-0011 update queue unchanged: only the Build Plan (v4) remains.**
  - **User elected to close out the Build Plan update in this same chat** rather than starting a fresh one — one-document-per-chat is a default, not a hard rule, and the remaining item was small and tightly scoped.
  - **Build Plan updated for ADR-0011.** Three changes, matching the original affected-documents entry:
    1. Step 3.1's wrapper bullet rewritten from "Claude Agent SDK wrapper... used by all stages except Stage 3" to describe the base `anthropic` Messages API client per ADR-0011 (no `allowed_tools`, explicit `max_tokens`, maintained per-model rate table for `total_cost_usd`). Folded in a callout for Document 6's try/except hard requirement, since it directly affects how every 3.2-onward stage script must be written and this is the step where a builder would first need to know that.
    2. New step **3.1a** added: commit the actual ADR-0011 text to `core/decisions/0011-base-anthropic-client.md`, noting explicitly that this is the first ADR added organically mid-build (as opposed to ADR-0010, which was folded into the seed list before Phase 1 ran).
    3. Step 8.3 reworded rather than a blunt "ten→eleven" replace. This resolved a genuine internal inconsistency in this context doc's own prior wording: one note (under the ADR-0011 subsection) called ADR-0011 "an organic addition BEYOND the original ten-seed-ADR list," while the Build Plan's own affected-documents entry said the seed count "needs to become eleven" — both true under different readings (ADR-0011 isn't one of the Phase-1 seed stubs, but by Phase 8 the real count in `core/decisions/` is eleven). Step 8.3 now says both things explicitly instead of collapsing to one framing.
  - **Also caught in passing:** the Document List's Build Plan row still credited chat 13 (`FORGE_Build_Plan_v2.md`) as current, despite the actual project file already being at v3 since chat 20's Phase 2 correction — same drift pattern as Documents 2, 3, and 6 earlier this session. Corrected.
  - Produced `FORGE_Build_Plan_v4.md`. File is in `/mnt/user-data/outputs/`.
  - **ADR-0011 document queue is now fully closed.** Documents 2, 3, 6, 9, and the Build Plan have all been updated and cross-verified against real code (`CLAUDE.md`, `requirements.txt`, direct repo greps via Claude Code) rather than assumption alone.
  - Next: Phase 3 resumes at step 3.2 (Intake Agent) — but first, complete step 3.1a (commit ADR-0011 to `core/decisions/`) if it hasn't happened yet, since it's now formally part of the plan. Start a fresh chat for the Phase 3 build work itself.

---
- **2026-07-29 (chat 26 — this chat):** Phase 3 build-work chat, as chat 25 recommended. Opened by checking the live public `forge-template` repo directly (raw file fetches + a live GitHub API check) rather than trusting the docs alone — confirmed step 3.1 genuinely complete (all five smoke tests still passing per `CLAUDE.md`), and confirmed **step 3.1a was NOT actually done** (`core/decisions/0011-base-anthropic-client.md` returned 404). Also found `CLAUDE.md`'s "Outstanding Before Phase 3 Continues" section was stale — still flagged Document 4/ADR-0010 as unresolved, which Document 4 v2 already has.
  - **Closed step 3.1a:** committed `core/decisions/0011-base-anthropic-client.md` (see the ADR-0011 subsection above for the RFC-field reasoning), and fixed `CLAUDE.md`'s stale Outstanding section in the same commit.
  - **Built and verified step 3.2 (Intake Agent)** — `core/agents/intake_agent.py`. Designed against the *actual* live signatures of `read_xlsx()`, `invoke_agent()`, `post_comment()`, `add_label()` (fetched directly from the public repo rather than assumed from `CLAUDE.md`'s summary of them). System prompt asks for 5–7 targeted clarifying questions; `invoke_agent()` call wrapped in try/except per the ADR-0011/Document 6 hard requirement, posting a failure comment on error. `--dry-run` flag added for local testing without touching GitHub.
    - **Two real bugs found and fixed during testing** (see the new build-notes subsection above for the fix details): the bracketed-placeholder-text bug in `read_xlsx()`, and the Windows `UnicodeEncodeError` on emoji in dry-run stdout.
    - **Real gap found:** no step anywhere (Phase 1 or Phase 4) creates the GitHub labels the pipeline depends on, and Document 6's own Label Reference table is missing two of them (`clarification-pending`, `design-approved`) — confirmed by cross-referencing Build Plan step 4.4's actual trigger condition. User created all nine needed labels manually via `gh label create`.
    - Verified end-to-end against a real "FORGE Pipeline Request" issue (#2, `forge-template`) — both a dry-run and a real run produced 6 well-targeted questions each (different questions between runs — expected model variance, not a bug); the real run's questions included a genuine FOIP/records-retention concern the model surfaced on its own from LAA context, unprompted. Comment posted correctly, `clarification-pending` applied correctly. **Step 3.2 confirmed complete.**
  - **Built and verified step 3.3 (Requirements Agent)** — `core/agents/requirements_agent.py`. Required three foundational additions first, done as a small retrofit pass (Part A): the agent-comment marker (retrofitted onto `intake_agent.py`'s comment), `get_issue_comments()` added to `github_helper.py`, and `_format_overview`/`_format_requirements` promoted out of `intake_agent.py` into `file_io.py` as public functions (`format_overview_markdown`/`format_requirements_markdown`) to avoid duplicating them. See the new build-notes subsection above for full details on all of these, plus the single-structured-JSON-call design, the ADO payload shape, and the direct-commit-to-`main` exception (flagged, not resolved, as a real tension with ADR-0009's general framing).
    - Verified `ado_helper.py`'s actual `create_epic`/`create_feature`/`create_user_story`/`create_bug` signatures directly from the live repo before designing the payload shape around them (all four exist, auto-link parent-child relationships via `link_items()`).
    - Verified end-to-end against issue #2: a real BA-style answer comment was posted first, then a dry-run reviewed (all 4 requirement rows → 4 User Stories, sharpened acceptance criteria reflecting the clarification answers, not just a restatement of the spreadsheet), then a real run confirmed `requirements.md` and `ado-work-items.json` both committed to `forge-demo-apps` at `docs/REQ-2026-01/`, and the draft summary comment posted to issue #2 with the correct marker and — correctly — no label applied (approval is a human action). Feature groupings differed slightly between the dry-run and real run (expected model variance on an open-ended grouping task); `source_req_number` traceability stayed 1:1 both times. **Step 3.3 confirmed complete.**
  - **Two things deliberately flagged and deferred rather than resolved this chat** (both added to Open Questions above): (1) a Phase 4 design recommendation — step 4.3's ADO item creation should fail the whole workflow run closed if any item fails, so Design can never start against a partial traceability chain; (2) a genuine governance gap — no process exists for a User Story changing after ADO items already exist, needs its own Document 6/Governance session.
  - **Two further documentation gaps confirmed real but NOT fixed this chat** (per the one-document-per-chat convention — this was a build-work chat, not a doc-update chat): Document 6's Label Reference table (missing two labels) and the Build Plan's missing label-creation step. Both need their own dedicated update chats — see Document List table above.
  - **`CLAUDE.md` needs a comprehensive update reflecting this entire chat** (3.1a/3.2/3.3 completion, the new conventions, the two bug fixes) — user is handling this directly via Claude Code rather than through this chat.
  - Next: Phase 3 continues at step 3.4 (Design Agent) — the first stage to use the full `create_branch()` → `commit_files()` → `open_pr()` chain (Requirements committed straight to `main`; Design opens a real PR against `design/<request-id>`), and the first to read `team/stack-preferences.yaml` as an input, which hasn't been verified to actually exist or match the shape the Build Plan describes yet. Start a fresh chat.
- **2026-07-29 (chat 27 — this chat):** Phase 3 build-work chat for step 3.4 (Design Agent), continuing directly from chat 26 rather than starting fresh — one-phase/one-chat is a default, not a hard rule, and this was a natural continuation of the same build arc. Opened by reviewing the context doc and `CLAUDE.md`, then fetching the live `forge-template` repo directly (`github_helper.py`, `file_io.py`, `claude_agent_wrapper.py`, `requirements_agent.py`, `team/stack-preferences.yaml`, `team/config.yaml`) rather than trusting the docs alone. `CLAUDE.md` in the live repo matched the project copy byte-for-byte — no drift found this time.
  - **Two real findings surfaced before writing `design_agent.py`**, both now documented in full under the new Phase 3.4 build-notes subsection above: (1) `github_helper.py` had no function to read a file from the monorepo — needed because Design is the first stage to consume an artifact (`requirements.md`) that a prior stage committed elsewhere, rather than a local input; fixed by adding `get_file_contents()`. (2) `team/stack-preferences.yaml`'s team-choice fields are still literal template placeholders in the live repo (`your-css-approach` etc.) — same category of problem as the Step 3.2 bracket-placeholder bug, fixed the same way (detect the placeholder marker, tell the model explicitly it's unset) via a new `format_stack_preferences_markdown()` in `file_io.py`.
  - **Built and verified step 3.4 (Design Agent)** — `core/agents/design_agent.py`. One structured-JSON `invoke_agent()` call producing `design_markdown`, `openapi_yaml`, and `tasks_markdown` together (same reasoning as Requirements Agent: cheaper, keeps the three documents consistent). `openapi_yaml` validated with `yaml.safe_load()` before committing. `_MAX_TOKENS = 20000` (vs. 8000 for Requirements) to cover three documents including a full API spec in one response.
    - **Dry run:** 2,929 input / 12,745 output tokens, `total_cost_usd: $0.1999` (arithmetic checked: 2,929×$3.00 + 12,745×$15.00 = $0.199962, matches), 207 s, `end_turn` (no truncation). All three artifacts valid: `design.md` (C4 narrative, 7 backend + 3 frontend components, tech choices table with exactly the six unset team-layer fields flagged and none of the seven real ones), `openapi.yaml` (passed validation; 4 endpoints), `tasks.md` (8 backend + 5 frontend + 8 test tasks, all tracing to requirement/component IDs).
    - **Real run against issue `forge-template#2`, request-id `REQ-2026-01`:** 2,929 input / 12,738 output tokens, $0.1999, 222 s. `design/REQ-2026-01` branch created from `main`; `design.md`, `openapi.yaml`, `tasks.md` committed in one commit; draft PR opened (`forge-demo-apps` PR #4) cross-linking `forge-template#2`; summary comment posted to issue #2 with the agent-comment marker, a working PR link, and instructions to mark the PR ready, merge it, and apply `design-approved`. **Step 3.4 confirmed complete.**
    - **`smoke_github.py` extended to 8 checks** (from 7) — added a round-trip read via the new `get_file_contents()`, verified against the file the smoke test's own `commit_files` call had just written. Clean run.
  - **One thing observed but deliberately not fixed, folded into the existing deferred Document 6 item:** agent-opened PRs are draft by default (ADR-0009), so the Technical Approver must click "Ready for review" before GitHub allows merging — a small extra step neither Gate 2's nor Gate 3's Document 6 text currently mentions. Same future dedicated Document 6 chat that needs to add `design-approved`/`clarification-pending` to the Label Reference table should pick this up too.
  - **Caught in passing while updating this document:** the "Pipeline Stages (summary)" table and "Agent Roster" section were both still labeling every non-Stage-3 stage as an "(SDK call)", stale since ADR-0011 (chat 21) switched those six stages to the base `anthropic` Messages API client. Corrected to "Messages API call" in both places — content-only terminology fix, no structural changes. (Historical ADR-0010 text and the chat-11 session note deliberately left as-is; they're verbatim/dated records of the pre-ADR-0011 state, not current-state claims.)
  - Next: Phase 3 continues at step 3.4a (Implementation Coordinator) — the first Managed Agents build (coordinator + Backend/Frontend/Test Writer subagents on a shared sandbox filesystem, per ADR-0010). This is a meaningfully different build than 3.2–3.4 (Managed Agents REST lifecycle via `managed_agents_wrapper.py`, not the Messages API wrapper) — recommend starting a fresh chat, pasting in this context doc.

---
- **2026-07-30 (chat 28 — this chat):** Phase 3 build-work chat for steps 3.4a–3.7 (Implementation Coordinator + Backend/Frontend/Test Writer subagents), continuing the Phase 3 build arc chat 27 recommended. Opened by reviewing the context doc and Build Plan, then live-checking the `forge-template` repo directly rather than trusting docs alone — confirmed `managed_agents_wrapper.py`/`smoke_managed_agents.py` existed and passed, but `implementation_coordinator.py` genuinely didn't exist yet (404), matching what the docs said. **Caught in passing:** `CLAUDE.md`'s own "Outstanding Before Phase 3 Continues" section mislabeled the next step "3.5" instead of the Build Plan's correct "3.4a" — minor, self-corrected once Mike's own Claude Code CLI updated `CLAUDE.md` at the end of this session.
  - **One real architectural gap found before writing any code, researched against Anthropic's current docs (Managed Agents postdates this assistant's training cutoff, so verified via search rather than assumed):** `managed_agents_wrapper.py` had no way to retrieve files the Backend/Frontend/Test Writer subagents write into their shared sandbox filesystem — the sandbox is Anthropic-hosted, invisible to FORGE's Python layer except via the **Files API** (`GET /v1/files?scope_id=<session_id>` + `GET /v1/files/{id}/content`, requiring **both** the `files-api-2025-04-14` and `managed-agents-2026-04-01` beta headers together, comma-separated). Same category of foundational retrofit as `get_issue_comments()` (chat 26) and `get_file_contents()` (chat 27) before their respective stage scripts.
  - **A second, sharper problem also researched rather than assumed:** every example found in Anthropic's own docs/cookbook only showed flat single output files — nothing confirmed whether nested directory structure under `/mnt/session/outputs/` survives in the Files API's `filename` field or gets flattened. Rather than build the commit step on that assumption, the coordinator is instructed to package the **entire** `services/<request-id>/` tree into a single `implementation.tar.gz` before finishing; Python's `tarfile` module (not the model) then guarantees exact path fidelity when reconstructing the `files: dict[str, str]` `commit_files()` expects. Sidesteps the open question entirely rather than risking silently-lost structure or filename collisions across folders.
  - **Retrofit: `managed_agents_wrapper.py` gained `list_session_output_files(session_id)` and `download_file_content(file_id)`**, plus a `_files_headers()` helper combining both beta headers. **Verified via an extended `smoke_managed_agents.py`** (10/10 checks, up from 6/6) before trusting it in a real, expensive coordinator run: a real specialist subagent wrote a file to `/mnt/session/outputs/`, and list + download + exact byte-for-byte content match were all confirmed against the real API.
  - **Built `core/agents/subagents/` (Backend, Frontend, Test Writer)** — each a small config module (system prompt + `DEFAULT_SCOPED_TOOLS`, shared via `subagents/__init__.py`), not standalone scripts, per the Build Plan's framing (subagents are Managed Agents specialist definitions the coordinator references, not independently invoked). Test Writer's prompt explicitly tells it to read Backend/Frontend's real files off the shared filesystem directly rather than take anyone's word for what's there — Backend and Frontend's own prompts do **not** have this same "read the real thing" instruction; they receive `design.md`/`openapi.yaml`/`tasks.md` only via the coordinator's relay in its delegation message. **Flagged as a real gap, not yet fixed:** for a structured contract like `openapi.yaml`, an LLM-to-LLM relay risks silent drift (a dropped field, a renamed property) that neither subagent could catch without the literal source to check against. This run's coordinator integration-check did confirm routes/schemas/error-codes/auth-headers aligned between Backend and Frontend, so no drift actually occurred this time — but the fix (write the three docs to the shared filesystem as the coordinator's first action; have Backend/Frontend read them directly, same as Test Writer already does) should land before a real commit, not after another dry run happens to also come up clean.
  - **Built `core/agents/implementation_coordinator.py`.** Reads `design.md`/`openapi.yaml`/`tasks.md` from `main` via the existing `get_file_contents()`, runs the coordinator session via the wrapper's existing `run_implementation_stage()`, downloads + extracts `implementation.tar.gz` via the new retrofit functions, commits to `feature/<request-id>`, opens a draft PR. Tracking-issue comment deliberately does **not** ask the approver to apply any label (unlike Design's `design-approved` instruction) — confirmed via the Build Plan that QA/Security trigger on "feature PR opened," not a label, so Gate 3's only human action is review + merge; QA/Security already run automatically the moment the PR opens.
  - **A false-alarm worth recording since it nearly caused duplicate/inconsistent code:** Claude Code CLI initially reported `run_implementation_stage` didn't exist in `managed_agents_wrapper.py`. Rather than reconstruct it from scratch, checked directly (`git log HEAD..origin/main`, then `Select-Story`/`grep` on the actual file) — the function was there all along (line 526), a false read on Claude Code CLI's first pass, not a real gap. Good example of the project's "verify, don't assume" discipline paying off.
  - **Claude Code CLI's own background-task tracker proved unreliable multiple times this session** ("Task ID is required" / "Waiting for task" appearing with no useful signal) — confirmed via a search of Anthropic's own GitHub issues that this is a known, open category of bug in the CLI's `TaskOutput`/background-task system, not specific to this machine. Real signal came from checking independently: `Get-Process python` (is it alive, is CPU ticking forward) and the Claude Console's session status (`running`/`idle`/`terminated`) — both stayed reliable throughout and should be the go-to checks next time the CLI's task tracker looks wrong.
  - **A real mid-run scare, handled correctly:** account balance went negative (-$0.29) partway through the coordinator session (which had already cost ~$3 at that point). Advised topping up immediately rather than waiting, since a negative balance risks orphaning the session (every subsequent API call — including the script's own `finally`-block cleanup — would fail, potentially leaving the session accruing session-hour runtime with nothing able to stop it). Mike topped up; the session's own poll calls either missed the bad window or tolerated it, and the run completed cleanly with no orphaned resources.
  - **Dry run against `REQ-2026-01` succeeded end-to-end.** All 4 agents (Opus coordinator + 3 Sonnet subagents) ran to completion; coordinator's integration check confirmed API routes/schemas/error codes/auth headers aligned between Backend and Frontend; `implementation.tar.gz` (156,728 bytes) downloaded and extracted cleanly to 96 files under `services/REQ-2026-01/backend/` and `.../frontend/`; session archived cleanly (all 4 agent resources + environment). Backend produced a full .NET solution (DocumentApi + EmailWorker projects, EF Core migrations, controllers, services, xUnit unit + integration tests); Frontend produced Next.js/TypeScript (upload page, history page, Tailwind/shadcn UI, React Query, Jest tests, Playwright e2e specs). Also produced CI workflow files and a compliance checklist that nothing in this build's system prompts asked for.
  - **Real cost data captured for the first time for Stage 3** (Document 3's "track actuals during App 1" open item): **$7.65 total**, `active_seconds: 2309` (~38.5 min). Usage breakdown (from the Console session detail page, not yet confirmed available via the wrapper's own API calls): `cache_read_input_tokens: 11,868,067`; `cache_creation` (5m): `358,148`; `input_tokens` (genuinely fresh): `253`; `output_tokens: 125,693`. **Confirms Managed Agents sessions cache automatically** — no `cache_control` was ever set anywhere in `managed_agents_wrapper.py`, yet cache reads dominate the token bill by four orders of magnitude over fresh input. Bounded what the same 12.2M cached tokens would have cost at full base-input rate (~$37–$61) against the actual $7.65 — roughly **80–88% saved**, consistent with Anthropic's "up to 90%" claim but now a real number instead of a guess. **Not yet confirmed:** whether this `usage` data is available from `GET /v1/sessions/{id}` (which `poll_until_idle()` already calls) or only surfaced via the Console — Mike was going to check `final_status` from a captured run, not yet done at end of session.
  - **Four review items surfaced but deliberately NOT checked this chat** (session moved on to other things before returning to them) — must be resolved before a real (non-dry-run) commit:
    1. Whether any archive member was silently skipped for being outside the expected `services/REQ-2026-01` prefix (`_extract_archive_to_file_dict()` logs a warning, not an error, on this — doesn't fail the run) — check the dry run's terminal log for `outside expected prefix`.
    2. Whether any `bin/`/`obj/`/`node_modules/`/`.next/`/`dist/` build artifacts made it into the 96 extracted files despite the coordinator's packaging instructions excluding them.
    3. **The CI workflow files and compliance checklist are scope creep** — nothing in the coordinator's or any subagent's system prompt this chat asked for either. Need to see their actual content/location before deciding whether to keep, edit, or strip them; a self-authored CI workflow inside `services/<request-id>/` could conflict with or duplicate the real Phase-4 pipeline wiring.
    4. Whether `EmailWorker` (a second backend project Backend produced, alongside `DocumentApi`) traces back to something real in `design.md`/`tasks.md`, or is functionality Backend added beyond what was asked.
  - **New follow-up queued, not yet actioned:** neither `claude_agent_wrapper.py` nor `managed_agents_wrapper.py` set any explicit `cache_control` — confirmed automatic caching is carrying Stage 3 extremely well regardless, but the Messages-API stages (Intake/Requirements/Design/QA/Security/Deploy) get no such benefit automatically and currently cache zero tokens on every call. Two small follow-ups queued: (1) add explicit `cache_control` to `claude_agent_wrapper.py`'s system prompts — modest win, only helps when two same-stage calls land within the cache TTL; (2) check whether `agents.create()`'s `system` field takes the same `cache_control` shape, and extend `get_subagent_audit_trail()` (or find another mechanism) to surface cache token counts per Stage 3 run going forward, closing the "zero cost visibility for Managed Agents" gap for good rather than relying on manually reading the Console each time.
  - **The actual generated implementation (the 96 files) does not exist anywhere durable** — it lived only in the now-archived sandbox and the now-exited Python process's memory; the dry-run print only logged paths/character-counts, not contents. Nothing was committed to git or GitHub. The only way to actually inspect the generated code (for the four review items above) is either the read-only Console session trace, or a real (non-`--dry-run`) run, which would commit to `feature/REQ-2026-01` and open a real, inspectable draft PR.
  - **Code committed and pushed this chat:** the retrofit to `managed_agents_wrapper.py`, the extended `smoke_managed_agents.py`, `core/agents/subagents/` (all three), and `core/agents/implementation_coordinator.py`. **`CLAUDE.md` updated by Mike's own Claude Code CLI workflow** (his standing practice — always re-posted to this project at the start of the next session) rather than by this chat.
  - Next: resolve the four open review items (ideally via the Console trace on the just-completed dry run, or accept them as open and check them against a real run's PR diff instead), fix the Backend/Frontend direct-file-read gap in their system prompts, then do the real (non-dry-run) run against `REQ-2026-01` — `feature/REQ-2026-01` branch, real draft PR, real tracking-issue comment. That's the point at which 3.4a–3.7 can genuinely be marked complete; it is **not** complete as of this chat's end, only dry-run-verified with known open items. Start a fresh chat for that work.

---
- **2026-07-30 (chat 29 — this chat):** Closed out steps 3.4a–3.7 for real. Opened by pulling the actual current source of `backend_agent.py`, `frontend_agent.py`, `test_writer_agent.py`, `implementation_coordinator.py`, `subagents/__init__.py`, and `managed_agents_wrapper.py` directly from the public `forge-template` repo (`raw.githubusercontent.com`) rather than trusting chat 28's summary of them — confirmed the relay gap chat 28 flagged was real: Backend/Frontend's system prompts said only "the coordinator will give you design.md, openapi.yaml, and tasks.md," no direct-read instruction, while Test Writer already had one (for code, not docs).
  - **Patch applied (Mike's Claude Code CLI, on his machine):** added `SHARED_DOCS_DIR = "/mnt/session/shared-docs"` to `subagents/__init__.py` as a single source of truth for the path; `_COORDINATOR_SYSTEM_PROMPT` gained a new step 0 instructing the coordinator to write `design.md`/`openapi.yaml`/`tasks.md` there *before* delegating (existing steps renumbered 1→6); Backend/Frontend system prompts now say to read those files directly from `SHARED_DOCS_DIR` rather than trust the coordinator's relay/paraphrase — same "read the real thing" pattern Test Writer already used for code. `SHARED_DOCS_DIR` deliberately sits outside `service_root` (`services/<request-id>`), so it's never swept into `implementation.tar.gz` by the existing exclude logic — confirmed no change needed to the packaging step. Committed and pushed to `forge-template`.
  - **Real (non-dry-run) run executed against `REQ-2026-01`.** Command: `python -m core.agents.implementation_coordinator --issue-number 2 --request-id REQ-2026-01` (no `--dry-run`). Design Agent's PR #4 was already merged to `main`, satisfying the coordinator's prerequisite.
  - **Real cost/duration captured — notably higher than the dry run, and now attributable:**

    | | Dry run (chat 28) | Real run (chat 29) | Δ |
    |---|---|---|---|
    | `active_seconds` | 2,309 (~38.5 min) | 3,310.6 (~55.2 min) | +43% |
    | cost (`list_cost`) | $7.65 | $12.31 | +61% |
    | `cache_read_input_tokens` | 11,868,067 | 17,512,129 | +48% |
    | `cache_creation` (5m) | 358,148 | 604,497 | +69% |
    | `output_tokens` | 125,693 | 235,190 | +87% |
    | `input_tokens` (fresh) | 253 | 239 | ~flat |

    Same request, same coordinator/subagent split — the jump is attributed to the shared-docs patch itself: Backend/Frontend now make an extra real read of `design.md`/`openapi.yaml`/`tasks.md` off the sandbox filesystem instead of getting the content relayed in one message, which costs more turns/output tokens per subagent. Logged as a known, accepted tradeoff (correctness against relay-drift vs. ~60% more cost per Stage 3 run) — not a bug, not yet re-optimized.
  - **A genuine operational incident, handled correctly:** the local script driving the real run was killed by Claude Code CLI's own background-task timeout mid-run — but the remote Managed Agents session kept running server-side, unaffected (same category of local/remote decoupling as chat 28's background-tracker unreliability, but with real financial stakes this time: had Mike/Claude Code CLI re-invoked the coordinator instead of checking, it would have spun up a **second**, duplicate, fully-billed coordinator + 3 subagents run on top of the one still in flight). Recovery was correct: wrote a small script reusing the existing `session_id` to fetch the finished output rather than re-running. **New standing rule, should also land in `CLAUDE.md`:** if a Stage 3 run's local process dies mid-flight, always check the session's status in the Claude Console (or via the wrapper's own session-status call) before considering a re-run — reuse the existing session ID to retrieve output; never re-invoke the coordinator on the assumption the first attempt is dead, since the remote session frequently isn't. A related, smaller follow-up: the coordinator script doesn't currently redirect its own stdout to a log file, which is why chat 28's and this chat's review-item-1 checks both had to rely on scrollback/live-tool-output rather than a saved log — worth adding by default for future long Stage 3 runs.
  - **Pipeline completed:** PR #5 opened on `forge-demo-apps` (`https://github.com/Flamespiker/forge-demo-apps/pull/5`), 101 files committed to `feature/REQ-2026-01` (up from the dry run's 96 — plausibly normal run-to-run variance, or one more real file each subagent produced as a result of the shared-docs patch giving them an additional genuine read to act on; not itself a concern once weighed against the review-item findings below), summary comment posted to tracking issue `forge-template#2`, all Managed Agents resources (session, environment, coordinator, 3 subagents) archived cleanly.
  - **All four review items from chat 28 now closed, checked against the real PR diff in `forge-demo-apps` (not `forge-template` — the two are separate repos, and `feature/REQ-2026-01` only exists in the monorepo; this tripped up the first few check attempts before landing on the right repo/ref):**
    1. **Archive members outside expected prefix — clean, with strong positive evidence.** Zero occurrences of the `outside expected prefix` warning across three separate extraction attempts (the original killed run, `diagnose_tree.py`'s extraction, and the final `resume_implementation.py` run), two of which were independent downloads of the same `implementation.tar.gz` — both produced identical 101 files / 101 unique paths with no conflicts. A silently truncated or mis-prefixed member would have shown up as either a warning or a count/path mismatch between the two independent downloads; neither happened.
    2. **`bin/`/`obj/`/`node_modules/`/`.next/`/`dist/` leakage — clean.** No matches anywhere in the diff; the coordinator's tar exclude flags worked as intended.
    3. **CI workflow + compliance checklist — confirmed scope creep, stripped.** Three files present that no prompt asked for: `services/REQ-2026-01/COMPLIANCE_CHECKLIST.md` (188 lines), `services/REQ-2026-01/backend/.github/workflows/ci.yml` (170 lines), `services/REQ-2026-01/scripts/verify-compliance.sh` (167 lines). `ci.yml`'s path was doubly wrong — GitHub Actions only discovers workflows at the repo root's `.github/workflows/`, so this one would never have triggered regardless of whether it was kept; dead weight, not a functioning duplicate pipeline. All three removed via a follow-up commit (`git rm` × 3) pushed onto the existing PR #5 branch before merge.
    4. **`EmailWorker` traceability — legitimate, not scope creep.** `tasks.md` explicitly calls for `EmailWorker` as a second .NET project (a background Service Bus subscriber) in three separate places — Backend built what was actually asked for. **Genuinely new, smaller finding surfaced by this check:** `design.md` has **zero** mentions of `EmailWorker` — the architecture doc never describes a component that `tasks.md` (which should derive from it) requires in detail. Not an Implementation-stage problem; flagged as a Design Agent artifact-quality gap for whoever next revisits `design_agent.py` or Document 6's stage-2 output-quality guidance. Not actioned this chat.

  > **Correction (chat 34, 2026-08-05):** this finding was wrong. `git blame` on `design.md` shows every `EmailWorker` reference (C4 diagram box, container table row, Service Bus decoupling row, monitoring row, and a dedicated "5. Email Worker Component" section) traces to a single commit — `6f665629`, the Design Agent's original PR #4 merge, 2026-07-29 20:29:11 -0600 — with no later edits to the file at all. `design.md` documented `EmailWorker` fully from the start; this chat's substring check (or the manual read behind it) missed it. Retracted, not resolved — there was never a Design Agent artifact-quality gap here.
  - **PR #5 merged** — without a squash. Not corrected or investigated further this chat; if FORGE wants a standing squash-vs-merge-commit convention for Stage 3 PRs (or any pipeline-stage PR), that's an open, undecided question, not yet a policy either way.
  - **Code committed and pushed this chat (via Mike's Claude Code CLI, on `forge-template`):** the `SHARED_DOCS_DIR` shared-docs patch across `subagents/__init__.py`, `backend_agent.py`, `frontend_agent.py`, `implementation_coordinator.py`. **On `forge-demo-apps`:** the follow-up commit stripping `COMPLIANCE_CHECKLIST.md`/`ci.yml`/`verify-compliance.sh` from `feature/REQ-2026-01`, then PR #5 merged (non-squash) to `main`.
  - **New document created: `docs/FORGE-pipeline-cost-log.md`** (committed to `forge-template`). A living per-run cost ledger, separate from this context doc (chat-continuity) and Document 3 (planning-stage estimates) — one place actuals accumulate. Seeded with every real figure on record: Design's dry run ($0.1999, 207s) and real run ($0.199857, 222s); Implementation's dry run ($7.65, 2,309s) and real run ($12.31, 3,310.6s), with the cost delta between the two attributed to the shared-docs patch. Intake and Requirements both got rows too, honestly marked **cost not captured** — both ran real, verified end-to-end, before this log existed, so their actuals are a genuine gap rather than a zero. Also documents a real automation asymmetry worth closing eventually: the six Messages-API stages already emit a `forge_event: agent_invocation` JSON line automatically; Stage 3 (Managed Agents) has no equivalent, so both its rows were hand-copied from the Console — same root gap chat 28 flagged ("not yet confirmed whether usage data is available from `GET /v1/sessions/{id}`"), now written down in a place future sessions will actually see it before re-deriving it from scratch. Document 3's "track actuals during App 1" open item should eventually cite this log rather than re-estimating, once a few more runs accumulate — flagged as a future Document 3 chat, not actioned here.
  - Next: **Phase 3 continues at step 3.8, QA Agent** (`core/agents/qa_agent.py`, Build Plan v4 — confirmed via direct grep of the Build Plan, since `CLAUDE.md`'s own numbering has drifted from the Build Plan's before, chat 28). Recommend starting a fresh chat for that build, pasting in this context doc. When `qa_agent.py` runs for real, add its actuals to `FORGE-pipeline-cost-log.md` §2 (and Intake/Requirements' actuals too, if a convenient real run of either happens to come up first, to close the cost-not-captured gap). Two other small follow-ups queued from this chat, not yet actioned: (1) add default stdout-to-file logging to the coordinator script so a future review-item-1-style check doesn't depend on live scrollback; (2) decide whether `design_agent.py`'s output-quality expectations should require every component `tasks.md` references to also appear in `design.md`.

---

- **2026-08-03 (chat 30 — this chat):** Built and committed **Step 3.8, the QA Agent** (`core/agents/qa_agent.py`), plus two supporting extensions to shared utils. Code was authored and unit-tested (synthetic data only, no live calls) in this chat, then applied and committed via Mike's Claude Code CLI on `forge-template` — same two-tool split as every prior stage.
  - **QA Agent's architecture, resolved this chat:** runs `dotnet test` (TRX logger) and `npm test -- --ci --json` (Jest) against `--repo-path/services/<request-id>/{backend,frontend}`; parses both report formats; classifies each failure's severity via a **fixed, deterministic string-marker mapping** (Xunit.Sdk/Assert./expect(/etc. → `3 - Medium`, everything else → `2 - High`) — no LLM judgment call, per Document 3's explicit "not AI judgment" language for this field. Files one ADO Bug per failing test via `ado_helper.create_bug()`, plus a synthetic Bug when a whole suite fails to even produce results (distinguished from "tests ran, some failed" by checking for the results file's existence, not exit code — `dotnet test` exits non-zero either way). Claude (`invoke_agent`, one call) is used only to write the human-facing PR comment from already-decided results — matches the cost log's "planned: Messages API" note for this stage, and mirrors Design/Requirements' "Claude writes prose, Python owns the facts" split.
  - **Retry-limit tracking resolved statelessly (ADR-0002 — no agent retains memory between runs):** attempt number = 1 + count of the agent's own prior marked comments (`<!-- forge:agent-comment stage=qa request_id=... -->`) already on the PR, fetched fresh each run. Attempts 1–3 with failures → `qa-loop-back`; attempt 4+ → `qc-retry-limit-reached`; no failures → `qa-approved`. All three labels applied to the **FORGE tracking issue** (`forge-template`), per Document 6's Label Reference table — not the monorepo PR.
  - **Real, load-bearing gap found and deliberately not "fixed" here:** `ado_helper.create_bug()` requires a `parent_story_id` to link a Bug to its User Story, but **Phase 4's ADO item-creation wiring (step 4.3) has never been built or run for any request** — confirmed this chat, not assumed — so no real ADO User Story IDs exist anywhere yet. Resolved by: (1) making `create_bug()`'s `parent_story_id` optional (defaults to `None`, logs a warning and skips the link instead of raising); (2) `qa_agent.py`'s `_resolve_parent_story_id()` looks for a real ID under a `primary_user_story_id` key in `docs/<request-id>/ado-work-items.json`, returns `None` today (expected — that file is still Requirements Agent's pre-Phase-4 draft shape), and will start resolving automatically the moment Phase 4 writes a real ID there. **Whoever builds Phase 4 step 4.3 needs to know this**: it must write `primary_user_story_id` (an int) into that file for QA's linking to activate — no change to `qa_agent.py` will be needed when that happens.
  - **New cross-repo GitHub functions added to `github_helper.py`:** `post_pr_comment()` and `get_pr_comments()`, both using the GitHub App installation token against the monorepo (`forge-demo-apps`) — same auth context as `create_branch`/`commit_files`/`open_pr`/`get_file_contents`. Needed because the feature PR (where QA posts its report and counts retry attempts) lives in the monorepo, while every existing comment/label function (`post_comment`, `get_issue_comments`, `add_label`, `remove_label`) is same-repo-only against `forge-template` via `GITHUB_TOKEN`. Same category of foundational retrofit as `get_issue_comments()` (chat 26) and `get_file_contents()` (chat 27) — a gap found and closed at the point the next stage actually needed the capability, not before.
  - **Docs drift noted, not corrected this chat (code chat, not a doc chat):** Document 2 (`02-forge-architecture-document-v3.md`) describes QA's output as a PR **check run**; Build Plan v4 and Document 6 Gate 4 both say PR **comment**. Followed Build Plan v4 as the authoritative step spec (same resolution pattern as prior doc-drift findings) — Document 2's wording should be corrected in a future Document 2 chat.
  - **Verified this chat:**
    - Unit tests (synthetic TRX XML, synthetic Jest JSON, mocked `invoke_agent`/`get_pr_comments`/`get_file_contents`/`post_pr_comment`/`add_label`/`ado_helper.create_bug`) confirmed: TRX and Jest parsing both correct; severity classifier correctly splits assertion-style vs. exception-style failures in both frameworks; full control flow correct across all four paths (attempt 1 → `qa-loop-back`, attempt 4 → `qc-retry-limit-reached`, all-pass → `qa-approved` with zero bugs filed, build/compile crash → one synthetic Bug filed); `--dry-run` confirmed to make zero GitHub/ADO calls.
    - `py_compile` clean on all three files (Mike's Claude Code CLI, real repo).
    - `smoke_ado.py` (4/4) and `smoke_github.py` (8/8) both still pass unchanged — confirms the `create_bug()` signature widening and the two new `github_helper.py` functions are additive, not breaking.
    - Confirmed against the real `forge-demo-apps` frontend `package.json`: `"test": "jest"` (bare script, no args of its own) — the assumption that `npm test -- --ci --json --outputFile=...` correctly forwards flags to Jest holds for this repo.
  - **Not yet done — genuinely open, not just untested (as of chat 30's end; resolved in chat 31, see below):** no live end-to-end run against a real feature branch. That needs a real checked-out `forge-demo-apps` working directory with actual backend/frontend code and tests in it (PR #5's content, from the Stage 3 real run) — mechanically possible today via `--repo-path` pointing at a manual local clone, since Phase 4's checkout wiring (step 4.5) isn't built yet either. Recommend that live run as the first thing in the next QA-related chat, before moving on to Step 3.9.
  - **Code committed and pushed this chat (via Mike's Claude Code CLI, on `forge-template`):** `core/agents/qa_agent.py` (new), `core/agents/utils/github_helper.py` (extended), `core/agents/utils/ado_helper.py` (extended). Commit `9c73c32`.
  - Next: **either a live dry/real run of `qa_agent.py` against PR #5's actual checked-out code** (recommended first, to validate the frontend-script assumption and the TRX/Jest parsing against real output, not just synthetic), **or proceed to Phase 3, Step 3.9: Security Agent** (`core/agents/security_agent.py`, Build Plan v4) if a live QA run isn't practical yet. When either QA or Security run for real, add actuals to `FORGE-pipeline-cost-log.md` §2. Start a fresh chat for that work, pasting in this context doc.

---

- **2026-08-04 (chat 31 — this chat):** First live runs of the QA Agent (`core/agents/qa_agent.py`) — both a dry run and a real (non-dry) run — against a real manual checkout of `forge-demo-apps`, per chat 30's recommendation. In the course of getting there, found and fixed several real defects spanning three different layers: an unpushed-commit gap, two cross-platform bugs in `qa_agent.py` itself, and five build/test-infrastructure defects in the merged application code that had nothing to do with QA logic but blocked the test suites from running at all.

  - **Chat 30's Step 3.8 work never actually reached GitHub.** `git log --oneline -5` on Mike's local `forge-template` clone showed three commits (`cc7056c`, `e616ee2`, `9c73c32`) sitting ahead of `origin/main` — the "committed and pushed" claim in chat 30's own session note was accurate about the local commit but wrong about the push. Confirmed via direct fetch against the live repo (raw file 404s, an empty `git ls-remote` branch list, a tree listing with `qa_agent.py` absent) before concluding anything — same "verify against live repo, not docs" principle already in this document's Key Learnings, now generalized: it's not just verbal "done" claims about credentials that need a second pass, unpushed commits are the same failure mode. Fixed with a plain `git push origin main`; re-verified via a fresh tarball pull that `qa_agent.py`, `github_helper.py`'s `post_pr_comment()`/`get_pr_comments()`, and `ado_helper.py`'s widened `create_bug()` signature were all genuinely present on `origin/main` before proceeding.

  - **Local test-suite prep surfaced five build/test-infrastructure defects in the merged App 1 code (`services/REQ-2026-01`) — none are QA findings, all are prerequisites that blocked `dotnet test`/`npm test` from running at all:**
    1. `DocumentApi.Tests.csproj` referenced `Microsoft.AspNetCore.Http.Features` at `8.0.0` — that package has published no stable NuGet releases since ASP.NET Core 3.0 (the types ship in the shared framework now); `dotnet restore` failed with NU1102. Fix: removed the reference; the types come through transitively via the existing project reference to `DocumentApi.csproj`.
    2. `EmailWorker.csproj` referenced `SendGrid` but not `SendGrid.Extensions.DependencyInjection`, the package that actually provides the `SendGrid.Extensions` namespace `Program.cs` needs for its `AddSendGrid()` DI call. Fix: added the missing package reference (`1.0.1`).
    3. `DocumentApiWebApplicationFactory.cs` called `AddInMemoryCollection()` on an `IConfigurationBuilder` without `using Microsoft.Extensions.Configuration;` — compiled fine on some setups because the extension method's namespace is easy to omit and still resolve via other usings, but failed here. Fix: added the missing `using`.
    4. `frontend/jest.config.ts` had `setupFilesAfterFramework` — not a real Jest config key (the correct one is `setupFilesAfterEnv`); Jest refused to even parse the TS config. The line originally had an adjacent comment asserting "Next.js docs spell this `setupFilesAfterFramework`" — likely the actual origin of the Test Writer subagent's error, and removed as part of the fix since it was not just superfluous but actively wrong once the key was corrected.
    5. `frontend/package.json` was missing `ts-node` (required for Jest to parse a `.ts` config file at all) and `@testing-library/jest-dom`, `@testing-library/react`, `@testing-library/user-event`, `@types/jest` (required for the test files' own imports/matchers).
    All five fixes went into a normal human-authored PR (`forge-demo-apps` **PR #7**, branch `fix/req-2026-01-test-infra`) — reviewed diff-by-diff in this chat (not just description-summarized) before merging, same discipline applied to `qa_agent.py`'s own fixes below. Merged to `main` as `fcae2b6`. **This is not FORGE-pipeline output** — it's meta-infrastructure repair on the repo itself, done via ordinary human PR review, not routed through Design→Implementation gates.
  - **After the fix-infra PR merged, retested against a genuinely clean checkout** (`git clean -xdf` on both `backend/` and `frontend/` to clear stale `bin/`/`obj/`/`node_modules`/`package-lock.json` left over from the earlier ad-hoc local patching, then fresh `dotnet restore`/`npm install`) to confirm the failure counts below are real application bugs and not artifacts of the patched-but-uncommitted state that produced them originally. **Confirmed identical: backend 71 passed / 2 failed (73 total); frontend 38 passed / 6 failed (44 total).**
    - Backend failures: `GetDocuments_TwoClientsSeeded_EachClientSeesOnlyOwnRecords` (expected 2 records, got 0 — client-scoping filter or seed-data issue); `PatchDocumentStatus_ValidKeyAndStatus_Returns200WithUnderReview` (expected `200 OK`, got `404 NotFound` — lookup-by-key mismatch).
    - Frontend failures: one likely test-assertion nit (`statusBadge_IncludesScreenReaderPrefix_ForAccessibility` — trailing-space mismatch between expected and rendered sr-only text); two real accessibility gaps (`fileSelection_UnsupportedMimeType_ShowsTypeErrorWithoutSubmitting`, `fileSelection_WordDocument_ShowsTypeError` — client-side mime-type validation doesn't render `role="alert"`, only the submit-time API-failure path does); three likely test-authoring issues, not app bugs (`submitForm_Success_DisplaysReferenceNumber`, `submitForm_Success_ClearsFormAfterUpload`, `submitForm_NetworkError_DisplaysGenericRetryMessage` — all fail on `getByText`/`getByRole` finding multiple matches, because the app legitimately renders both a visible message and a duplicate `sr-only aria-live` announcement for screen readers; tests need `getAllByText`/a scoped query, not a fix to the app). Not re-triaged individually by the QA Agent's deterministic classifier beyond pass/fail — worth a human second look at which of these 8 are real product bugs vs. test-authoring issues before they're worked, since the classifier doesn't distinguish the two categories.
  - **`e2e/` folder found in the checkout** (Playwright config + fixtures + tests) that wasn't part of the original plan — checked against `tasks.md` directly rather than assumed either way: **T-004 explicitly calls for Playwright E2E tests** (three separate mentions, including a Design Agent flag that Technical Approver should confirm the framework choice), so this is legitimate traceable output, not scope creep like chat 28's `COMPLIANCE_CHECKLIST.md` finding. Out of scope for `qa_agent.py` (which only runs xUnit/Jest) and can't run yet regardless — the tests target a staging environment with a seeded Azure AD test account, neither of which exists. No action needed; noting for whoever eventually wires up staging deploys.
  - **Two real, Windows-only bugs found and fixed in `qa_agent.py`'s `_run_shell()` helper** (`core/agents/qa_agent.py:152`), both diagnosed and fixed by Claude Code, diff reviewed line-by-line in this chat before commit:
    1. Bare `"npm"` in the subprocess argv doesn't resolve to `npm.cmd` on Windows — `CreateProcess` (what `subprocess.run` calls without `shell=True`) doesn't consult `PATHEXT` the way `cmd.exe` does. Fixed with `shutil.which(command[0]) or command[0]` before invoking — cross-platform safe, correctly returns the `.cmd` path on Windows, still fails normally if the binary is genuinely missing. Fixes both the `npm test` and `dotnet test` call sites in one place since both go through the shared helper.
    2. Jest's UTF-8 output (checkmarks, ANSI codes) crashed the subprocess reader thread with a `UnicodeDecodeError` under Windows' default `cp1252` locale decode. Didn't corrupt this run's actual result (the frontend parser reads Jest's JSON report file, not stdout) but would have crashed instead of degrading gracefully if a real build failure had ever needed the stdout/stderr tail for diagnostics. Fixed with explicit `encoding="utf-8", errors="replace"` on the `subprocess.run()` call.
    Both fixes are additive and Windows-local-only — no behavior change on the Linux GitHub Actions runners this is meant to run on eventually. `py_compile` clean. Committed and pushed to `forge-template` main directly (not a PR — same repo/same convention as chat 30's direct-to-main commits for `qa_agent.py` itself).
  - **Dry run (`--dry-run`, before the infra-fix PR existed) produced full, correct output on the first real attempt:** 11 backend tests, 44 frontend tests, deterministic bug list, Claude-authored PR comment draft, `qa-loop-back` recommendation (attempt 1 of 3) — validated the TRX/Jest parsing and severity classification against real (not synthetic) tool output for the first time. Numbers here are stale relative to the post-infra-fix retest above (2 backend + 6 frontend failures either way, but measured against the ad-hoc-patched, uncommitted checkout state, not truly clean merged code) — the retest's 71/2 and 38/6 are the trustworthy figures.
  - **Real run (no `--dry-run`) — first fully real, end-to-end QA Agent execution, run against the genuinely clean post-infra-fix checkout:**
    - **8 real ADO Bugs filed** in `spike99`/`FORGE-Build` (work items #96–103): 2 backend failures at `3-Medium`, 6 frontend failures at `2-High`. No `parent_story_id` link on any of them — expected, Phase 4's ADO item-creation wiring (step 4.3) still doesn't exist, exactly the documented chat-30 gap.
    - **Real PR comment posted** to `forge-demo-apps` PR #5.
    - **Real label applied** to `forge-template#2`: `qa-loop-back` (attempt 1), confirmed via API.
    - Claude call: 3,361 in / 947 out tokens, $0.024288, 15.58s — add to `FORGE-pipeline-cost-log.md` §2 (not yet done this chat — flagged as a follow-up).
  - **Timing gap surfaced by this run, not fixed:** PR #5 was already merged before this real run executed (its feature branch no longer exists; `main` already had the content). Posting a QA report and filing bugs against an already-merged, closed PR works mechanically but doesn't match the pipeline's intended "QA gates *before* merge" semantics. Accepted as a known limitation for this validation exercise; **Phase 4 design should specify QA (and Security) running on PR-open, before the merge gate, not after** — not actioned this chat.
  - **Implementation Coordinator's own tracking-issue comment overclaims automation that doesn't exist yet.** Its summary comment on `forge-template#2` states "QA and Security are already running in parallel — both trigger automatically now that this PR is open, and will post their own results directly on it." No such automatic trigger exists (that's Phase 4 wiring, not yet built) — this chat's QA results only exist because they were run by hand. A real Technical Approver reading that comment would wait indefinitely for something that isn't coming on its own. Needs a prompt fix to the Coordinator's comment template (hedge the claim, or defer it until Phase 4 automation actually exists) — not actioned this chat, flagged for whoever next touches `implementation_coordinator.py`'s comment-generation prompt.
  - **Label lifecycle gap noticed on `forge-template#2`:** `clarification-pending` (a Stage 0 label) is still present alongside the newly-applied `qa-loop-back`, despite the pipeline having progressed through Requirements/Design/Implementation/QA. No stage appears to clear prior-stage labels when applying its own — same category of gap as the existing Document 6 label-reference table issues already tracked, not actioned this chat.
  - **Code committed and pushed this chat:**
    - `forge-template` (direct to `main`, no PR): `core/agents/qa_agent.py` — `shutil.which()` executable resolution + UTF-8 decode hardening in `_run_shell()`.
    - `forge-demo-apps` (PR #7, `fix/req-2026-01-test-infra` → `main`, merge commit `fcae2b6`): the five build/test-infrastructure fixes listed above.
  - Next: **triage the 8 filed ADO Bugs** (#96–103) to separate real product bugs from test-authoring issues before assigning work, per the classifier-doesn't-distinguish note above; **add this run's Claude-call cost to `FORGE-pipeline-cost-log.md` §2**; decide on the QA-before-merge timing question for Phase 4 design; then either **Phase 3, Step 3.9: Security Agent** (`core/agents/security_agent.py`, Build Plan v4) or continue QA-related follow-ups (Coordinator comment-template fix, label-lifecycle fix) — Mike's call on ordering. Start a fresh chat, pasting in this context doc.

- **2026-08-04/05 (chat 32 — this chat):** Step 3.9 — Security Agent (`core/agents/security_agent.py`) built, tool-installed, verified, and **live-run confirmed** — the first stage to go from zero to a fully verified real run in a single chat, rather than split across two like QA's chat-30→chat-31. Committed and pushed in two commits: `405fd0c` ("Step 3.9: Security Agent (Stage 5)" — initial build, `github_helper.py` additions, `requirements.txt`) and `69a8ad2` (Gitleaks allowlist fix + polish, described below).

  - **Four new `github_helper.py` functions added** (appended, no existing function modified; `py_compile` clean):
    - `get_pr(pr_number)` — resolves a monorepo PR's head commit SHA. Nothing before this needed one; QA only ever posted a plain issue-style comment.
    - `create_check_run(head_sha, name, conclusion, title, summary)` — creates a completed check run on a commit. Uses the App's existing Checks: Read & Write permission (step 2.1, already granted, never previously exercised). Check name is `security-check`, matching Build Plan 4.8's branch-protection required-status-check.
    - `create_review_with_comments(pr_number, commit_id, comments, body="")` — batches inline (file+line) PR review comments in one call via the Reviews API.
    - `create_single_review_comment(pr_number, commit_id, path, line, body)` — per-comment fallback, since GitHub's Reviews API is atomic (one comment with a path/line outside the diff fails the *entire* batch, silently dropping every other legitimate finding otherwise).
  - **`core/agents/security_agent.py` created.** Runs three scanners via shell against `services/<request-id>` in a local checkout (same `--repo-path`-supplied-checkout assumption as QA — this script doesn't clone anything): Semgrep (SAST), Gitleaks (secrets), OWASP Dependency-Check (dependency CVEs). Same `_run_shell` / `shutil.which()` Windows-executable-resolution pattern QA already established; added `_dependency_check_executable()` since Dependency-Check (unlike npm/dotnet) ships as differently-named `.sh`/`.bat` scripts per platform rather than one bare name.
  - **Fixed, deterministic severity mapping per tool** (Document 7: locked Critical/High/Medium/Low schema, never an LLM judgment call — same "FORGE automatic, not AI judgment" discipline as QA's assertion-vs-crash classifier):
    - Gitleaks: every finding → Critical (no lesser-severity category of its own).
    - OWASP Dependency-Check: CVSS-threshold based (≥9.0 Critical, ≥7.0 High, ≥4.0 Medium, else Low; prefers `cvssv3.baseScore`, falls back to `cvssv2.score`; a vulnerability with neither present defaults to Medium, logged, rather than silently under-reporting as Low).
    - Semgrep: ERROR→High, WARNING→Medium, INFO→Low. **Documented limitation:** under this fixed table, Semgrep can never produce a Critical finding — Semgrep community rules don't expose a CVSS-equivalent score to threshold against. Same honesty-about-heuristic-limits pattern as QA's own severity classifier docstring.
  - **No retry-loop/attempt-counting**, unlike QA's 3-retry rule — Document 6 has no `security-loop-back` or retry-limit label for this stage. Security re-scans on every PR update; the failing `security-check` check run is what blocks merge until resolved, not a retry budget.
  - **Gate/label mechanics:** if any Critical finding exists, the check run conclusion is `failure` (this alone blocks merge via branch protection) and `security-approved` is **not** applied. If no Critical findings, check run conclusion is `success` and `security-approved` is applied directly by the agent — same agent-applies-the-label precedent as QA's `qa-approved`. **Same pre-existing Document 6 Label Reference table gap already flagged in chat 31 for `qa-approved`** ("Applied by: Security Reviewer" in the table, but the agent applies it in practice) — not re-litigated here, just continuing the same known, tracked discrepancy.
  - Claude is used once per run, only to write a short human-facing overview comment from already-computed deterministic counts (by severity and by tool) — it does not re-judge severity and does not write individual finding text; those are built deterministically (`_build_finding_comment()`) and posted as inline review comments separately from Claude's call.
  - **Inline comment posting has a three-tier fallback:** batch review via `create_review_with_comments()` first (one API call); on failure (most likely a finding whose line isn't part of the diff), falls back to `create_single_review_comment()` per finding; anything that still fails, plus any finding with no file/line at all (all OWASP Dependency-Check findings — manifest-level, not line-anchored), gets aggregated into one plain PR comment so nothing is silently dropped.
  - **KNOWN GAP (flagged, not fixed):** re-running this agent after a fix re-posts the full current finding set as new inline comments rather than diffing against a prior run's comments — no de-duplication against previously posted findings exists yet. Flagged as an open item for whoever wires Phase 4, same pattern as other gaps already tracked in this document.
  - **`requirements.txt` updated:** added `semgrep>=1.90.0` with a comment explaining Gitleaks and OWASP Dependency-Check are NOT pip-installable (separate binary installs). Verified: `pip install -r requirements.txt` succeeded, installing `semgrep-1.172.0`; `semgrep --version` confirmed it's on PATH.
  - **Incidental environment side effect, flagged (not a FORGE issue):** the `pip install` also downgraded two pre-existing packages already present in Mike's environment — `pywin32` 312→311 and `mcp` 1.29.0→1.23.3 (a transitive dependency semgrep pulled an older pin for). Neither package is declared in `requirements.txt`; this was pip's resolver adjusting what was already installed locally, not something the new semgrep line itself specifies. Shouldn't affect FORGE, but worth knowing if anything else in Mike's environment depends on the newer `mcp`.
  - **Synthetic-data verification performed early in this chat** (no live API/GitHub calls, mirroring QA's chat-30 discipline): 17 checks against the three parsers (`_parse_semgrep`, `_parse_gitleaks`, `_parse_dependency_check`) plus 6 checks against the orchestration-level decision logic (severity aggregation → check-run conclusion → label gating) and the three-tier inline-comment fallback partitioning. All 23 passed, including edge cases: a Semgrep result with a missing `extra` block (defaults to Medium, no crash), both `"[]"` and a truly empty Gitleaks report (0 findings, no crash), a Dependency-Check vulnerability with no CVSS score at all (defaults to Medium, no crash), and a dependency with an empty `vulnerabilities` list (contributes 0 findings).
  - **All three tools installed and confirmed on Mike's machine:** Gitleaks 8.30.1 (via `winget install --id Gitleaks.Gitleaks -e`), Java Temurin 21.0.12 LTS (`winget install --id EclipseAdoptium.Temurin.21.JDK -e` — required by Dependency-Check, wasn't previously installed), OWASP Dependency-Check 12.2.2 (manual zip install from `dependency-check/DependencyCheck` GitHub releases — no winget package exists for it).
  - **NVD database warmup hit a real, currently-open upstream issue:** the NVD API intermittently returns 503s during large bulk downloads, and Dependency-Check's retry logic has no backoff between attempts — a known bug filed against the project itself, not a local misconfiguration. Confirmed via direct `Invoke-WebRequest` to the NVD API returning 200 OK quickly, ruling out a local network/firewall problem. Resolved by simply retrying (progress is cached locally, so each attempt resumes further than the last) — the full sync (373,262 records) eventually completed cleanly. This is a one-time cost; future updates are small deltas. Flagged as an operational reality of this tool, not a FORGE defect.
  - **`_run_dependency_check()` patched to pick up an `NVD_API_KEY` environment variable**, passed via `--nvdApiKey` when set; if unset, the scan still runs but logs a warning about rate-limiting risk. Mike registered a free NVD API key (`https://nvd.nist.gov/developers/request-an-api-key`) and set it locally.
  - **Real `--dry-run` against merged `services/REQ-2026-01` code (already-merged PR #5, chosen deliberately as the simplest available target rather than spinning up a fresh test PR) surfaced a real false positive on the first attempt:** Gitleaks flagged `backend/DocumentApi.IntegrationTests/DocumentApiWebApplicationFactory.cs:33` — `public const string TestInternalApiKey = "test-internal-api-key-abc123"` — as Critical. Confirmed via manual inspection this is an obvious test fixture, not a real credential (name and value both clearly synthetic). Under the fixed "every Gitleaks finding = Critical" mapping, this would have blocked merge on real generated code purely for a completely normal .NET integration-test pattern (hardcoding a fake key in `WebApplicationFactory` setup) — recognized as something that would recur on every future generated service with integration tests, not a one-off.
  - **Fix: `team/gitleaks-allowlist.toml` added** — a team-configurable Gitleaks allowlist (per Document 7's Flexible/Locked model: Gitleaks itself is a Flexible tool choice, and `team/` is exactly where team-configurable-but-locked-path files already live, alongside `team/config.yaml` and `team/stack-preferences.yaml`) excluding any path containing a case-insensitive "test" directory segment. `_run_gitleaks()` passes `--config` pointing to this file automatically, with a graceful fallback (default ruleset + warning log) if the file's ever missing. Re-ran the dry-run after the fix: the false positive disappeared entirely (excluded before ever reaching Gitleaks' output, not suppressed after the fact), `counts_by_severity.Critical` → 0, `check_conclusion` → `success`, with no other findings surfacing as a side effect of the config change.
  - **First real (non-dry-run) live run completed successfully against PR #5 / issue #2:** posted a real overview comment to the PR, created a real `security-check` check run (conclusion `success`) on head commit `0f5f1c57`, and applied `security-approved` to tracking issue #2. All three artifacts independently confirmed by Mike directly on GitHub (not just trusting the script's own log output) — comment visible on the PR, `security-check` green on the commit, `security-approved` label present on issue #2 alongside QA's earlier `qa-approved`. Claude call: 599 in / 269 out tokens, $0.005832, 5.37s.
  - **Two smaller fixes landed alongside the allowlist commit** (`69a8ad2`): replaced two remaining literal em dashes in log messages with ASCII double-hyphens (console-codepage rendering had mangled them as `�` — same class of issue as `intake_agent.py`'s existing emoji-encoding fix; a `sys.stdout.reconfigure(encoding="utf-8")` fix alone doesn't control the *terminal's* codepage, so avoiding non-ASCII output entirely is the more durable fix). An earlier commit-message draft incorrectly described a `--dry-run` individual-findings printout and a `stderr` encoding fix that didn't actually match the code (Claude Code CLI itself caught the discrepancy against `git diff HEAD` before committing) — corrected before commit; flagged here as a reminder that commit messages need verifying against the actual diff, not just against what was *intended*.
  - **KNOWN GAP, still unbuilt:** the individual-findings printout for `--dry-run` (showing tool/severity/path/line/rule/message per finding, not just aggregate counts) was specified but never actually implemented in this chat — the prompt was drafted but the session moved on to the allowlist fix before it was applied. This is why the false positive above had to be diagnosed via a manual `gitleaks detect` command rather than the dry-run output itself. Real, still-open gap for a future session: `--dry-run` currently shows *that* something was found, not *what* it was.
  - **KNOWN GAP (flagged, not fixed):** re-running this agent after a fix re-posts the full current finding set as new inline comments rather than diffing against a prior run's comments — no de-duplication against previously posted findings exists yet. Flagged as an open item for whoever wires Phase 4, same pattern as other gaps already tracked in this document.
  - **`requirements.txt` updated:** added `semgrep>=1.90.0` with a comment explaining Gitleaks and OWASP Dependency-Check are NOT pip-installable (separate binary installs). Verified: `pip install -r requirements.txt` succeeded, installing `semgrep-1.172.0`; `semgrep --version` confirmed it's on PATH.
  - **Incidental environment side effect, flagged (not a FORGE issue):** the `pip install` also downgraded two pre-existing packages already present in Mike's environment — `pywin32` 312→311 and `mcp` 1.29.0→1.23.3 (a transitive dependency semgrep pulled an older pin for). Neither package is declared in `requirements.txt`; this was pip's resolver adjusting what was already installed locally, not something the new semgrep line itself specifies. Shouldn't affect FORGE, but worth knowing if anything else in Mike's environment depends on the newer `mcp`.
  - **CLAUDE.md update in progress, done by Mike directly (not in this chat):** covers the Semgrep install, the Gitleaks/Java/Dependency-Check installs, the NVD API key setup, and this session's live-run verification — matching the pattern used for other stage agents. Not yet reviewed against the live repo in this chat.
  - Next: **implement the still-missing `--dry-run` individual-findings printout** (flagged above); **triage/decide whether the Sonatype OSS Index Analyzer** (disabled — requires separate credentials not yet obtained) is worth adding later, given it checks a second vulnerability database beyond NVD. See the end-of-session document review below for the full queue of follow-up chats (documentation fixes + Step 3.10).

  **End-of-session project document review (this chat, 2026-08-05):** A full pass over every project document was conducted, checking each against what was actually built/verified in this session. Findings, queued as separate follow-up chats per the one-document-per-chat convention rather than fixed inline here:

  1. **`03_FORGE_Tooling_v7.md` — real architecture mismatch, not just stale wording.** The document describes Semgrep/Gitleaks/Dependency-Check running as three separate GitHub Actions marketplace steps (`returntocorp/semgrep-action`, `gitleaks/gitleaks-action`, `dependency-check/Dependency-Check_Action`), each independently posting its own results as check runs/comments. What actually got built is different: `security_agent.py` invokes all three directly via subprocess/shell as locally-installed CLI binaries, parses their output itself, and is the sole poster of every comment and check run — no marketplace actions are used anywhere. The document also doesn't mention the NVD API key requirement or the Java prerequisite, both of which turned out to be real, non-trivial setup steps this session. Same category of doc-assumed-one-architecture/build-settled-on-another gap as Stage 3's Messages API vs. Managed Agents split (ADR-0011) — likely worth its own ADR given it's a real deviation from a documented default, not a copyedit.
  2. **Three separate places assume a human applies `security-approved`, not the agent** — `06_Orchestration_v3.md` line 228 (Gate 5 narrative), `06_Orchestration_v3.md`'s Label Reference table ("Applied by: Security Reviewer"), and `FORGE_Build_Plan_v4.md` step 5.8. This is the same already-tracked gap flagged for `qa-approved` back in chat 31, but now showing up in three spots for Security instead of one — a pattern across both gated stages worth resolving with a single RFC/decision (do the docs get corrected to match built behavior, or does built behavior change to match the docs?) rather than continuing to patch it stage-by-stage as it's discovered.
  3. **`FORGE-pipeline-cost-log.md` needs real content added, not just corrections:** §1's reference table still lists Security as "(not yet built)"; §2 has no Security table at all yet (needs the two dry-run costs — $0.00654/8.83s and $0.005637/5.76s — plus the real run: $0.005832/5.37s, PR #5, commit `0f5f1c57`); §3's cumulative totals need updating accordingly. **Separately, pre-existing and not caused by this session:** the QA row in §1 still says "(not yet built — next step, 3.8)" even though QA was built and live-run-verified two chats ago (chat 30/31), and QA's real-run cost was never added to §2 despite being flagged as a to-do at the time (chat 31's own session note said "add this run's Claude-call cost to `FORGE-pipeline-cost-log.md` §2" — never actioned).
  4. **Bookkeeping observation, not urgent, not fixed:** `FORGE_Build_Plan_v4.md` has zero checked boxes across all 71 items, including Phase 1/2 and Steps 3.1–3.8 which are all done and verified. This looks like a document-wide pattern (progress lives in the context doc instead) rather than something specific to Security — flagged for a decision on whether to start checking boxes going forward, rather than unilaterally checking one and leaving the other 70 inconsistent.
  5. **Checked and confirmed fine, no update needed:** `02-forge-architecture-document-v3.md`, `01-forge-product-specification_v2.md`, `07_Customization_Ref_v2.md` (the Gitleaks allowlist is an implementation detail under the existing "Flexible" tool-choice rule — doesn't need its own doc line), `09-forge-readme_v6.md`, `04_Governance-v2.md`, `05_AI_Foundation_v2.md`, `ADR-0011.md`, `00-forge-introduction.md`, `doc2-change-brief.md`.

  - **Queued follow-up chats, still open, in recommended order:** (a) Document 3 tooling architecture rewrite (the substantive one — possibly its own ADR); (b) Document 6 + Build Plan label-applier decision, covering both `qa-approved` and `security-approved` together rather than patching piecemeal; (c) cost log fix (Security rows + the stale QA row) — smallest of the three, could be folded into whichever of (a)/(b) happens first, or done standalone. Step 3.10 (Deploy Agent) was picked up in chat 33 instead of these — see below.

---

- **2026-08-05 (chat 33 — this chat):** Phase 3, Step 3.10 — Deploy Agent scoping and Azure/local tooling setup. No code written yet this chat (that's Claude Code's job next); this session closed out every real-world prerequisite and produced a full implementation brief, following the same "resolve the open architectural question before writing code" pattern as prior stage kickoffs (e.g. Stage 3's Files API research in chat 27/28).

  - **Scope decision:** staging auto-deploy only. Production (GitHub Environment gated) deploy is explicitly deferred to a follow-up chat — not built, not stubbed beyond what's noted below.

  - **Open item from Document 3 §9 resolved:** "Container Apps deployment credentials (Deploy Agent) — not yet finalized" is now finalized for the staging path. Decision: a scoped Azure AD service principal with Contributor role, **not** the runner's own interactive `az` session and **not** a managed identity (managed identity was considered but doesn't fit — GitHub Actions runners aren't Azure-hosted compute, so there's no Azure resource identity to attach one to). This mirrors the ACR admin-user-credentials precedent from Phase 2.2 (build-phase-acceptable, narrower-scope-before-production is the same graduation path already used there).

  - **Real, unplanned architecture gap found before any code was written:** neither `design.md` nor any prior session ever assigned Dockerfile authorship to anyone. Backend/Frontend subagents (Stage 3) don't produce them; nothing in `core/agents/` does either. Confirmed via direct repo check — no `Dockerfile` exists anywhere under `services/REQ-2026-01/` in the merged PR #5 checkout. Decision: **Deploy Agent owns Dockerfile generation deterministically from stack templates**, not an LLM guess and not a Stage-3 subagent retrofit — consistent with the project's established pattern (QA's TRX/Jest parsing, Security's severity mapping) of keeping structural/format-shaped output out of the model's hands. Retrofitting Stage 3 was considered and rejected: it wouldn't help REQ-2026-01 (already merged without Dockerfiles), and adds a fourth thing Backend/Frontend could drift or scope-creep on, on top of the already-tracked `COMPLIANCE_CHECKLIST.md`/stray-CI-file issue from chat 28.

  - **A second gap surfaced by the first:** this build's real Backend output has *two* deployable .NET projects (`DocumentApi` + `EmailWorker`), not one — a fixed single-Dockerfile-per-service assumption doesn't hold. Deploy Agent needs **deterministic multi-unit detection**, not an LLM decision: walk `services/<request-id>/backend/` for `*.csproj` files (skipping any path containing "test", same convention as the Gitleaks allowlist), classify each as `web` (references `Microsoft.AspNetCore.App`/`Microsoft.NET.Sdk.Web`) or `worker` (references `Microsoft.Extensions.Hosting`, no ASP.NET reference); treat `services/<request-id>/frontend/package.json` as one additional `frontend` unit if present. Each unit gets its own name (`<request-id>-<unit-slug>`), its own generated Dockerfile (only if one doesn't already exist in that unit's directory), its own ACR image tag, and its own Container App.

  - **Design-doc traceability gap connected explicitly:** this is the same already-tracked `EmailWorker`-in-`tasks.md`-but-not-`design.md` gap flagged back in the Stage 3 build (chat 28) and never resolved. Rather than let Deploy Agent silently deploy a unit nobody signed off on in `design.md`, the spec requires it to **flag** (not block) any detected unit with no corresponding `design.md` entry, surfaced in the PR comment. Doesn't fix the underlying gap — keeps it visible at the one additional point in the pipeline where it could otherwise go unnoticed.

    > **Correction (chat 34, 2026-08-05):** the premise here didn't hold. The flag logic built from this brief (a literal camelCase-substring match) produced false positives on both units; fixing the matching logic to check spaced-prose forms as well showed `design.md` fully documents `EmailWorker` and always has (see the chat-28 correction above — single commit, no later edit). The flag mechanism itself was still worth building — it's a real, generalizable check for future stages/units — but there was no live gap for it to have been closing here.

  - **Azure setup performed and verified live this session, in order:**
    1. Azure CLI installed — **first attempt used the default `aka.ms/installazurecliwindows` link, which is the 32-bit MSI.** This surfaced a real, confirmed compatibility problem: `az extension add --name containerapp` failed with "Pip failed with status code 2" — root-caused via `--debug` output to `cryptography` (a `containerapp` extension dependency) having no prebuilt wheel for 32-bit Python 3.14.6, forcing a from-source build requiring a Rust/C toolchain that wasn't present. This is a known, long-standing Azure CLI Windows packaging issue (32-bit bundled Python on 64-bit Windows), not anything specific to this machine. **Fixed by uninstalling and reinstalling via the 64-bit-specific link, `aka.ms/installazurecliwindowsx64`** (confirmed via Microsoft's own docs this is a distinct, separate installer from the default link — the default link needs to be remembered as 32-bit for any future machine setup in this project). Re-verified via `az --version`: 64-bit Python (`Program Files\...` path, no `(x86)`, `64 bit (AMD64)`). `containerapp` extension then installed cleanly with no further issues (its "preview" warning is expected/normal, not an error).
    2. **`az login` initially failed** with an MFA-required error (`AADSTS50076`) — the default Windows auth broker flow didn't actually surface an interactive MFA prompt. Fixed with `az login --use-device-code`, which completes MFA normally through a browser. Confirmed subscription: **"Mikes Sub"** (tenant `af2dd50c-3bc0-4e26-9973-e3af4b64dbf9`, subscription `6ca6b79c-4e1d-477a-9b81-b91bf5ce5088`).
    3. **Confirmed via `az containerapp env list`:** both `forge-staging` and `forge-production` live in the **same resource group, `forge-build-rg`** (Canada Central) — this wasn't previously recorded anywhere in project docs (Phase 2.3/2.4 notes only said the environments were created, not which resource group). **Flagged, not resolved:** scoping the staging SP to `forge-build-rg` necessarily also grants it Contributor over `forge-production`, since Azure has no environment-level scope, only resource-group level — there's no way to isolate staging-only access with the current resource-group layout. Accepted as a build-phase tradeoff (one shared RG, one SP, simplest path) rather than fixed this session; splitting `forge-production` into its own resource group is the clean fix if this is worth resolving before the production-deploy follow-up chat, but that's a call for whoever picks that chat up, not decided here.
    4. **Service principal created:** `forge-deploy-staging`, Contributor role, scoped to `/subscriptions/6ca6b79c-.../resourceGroups/forge-build-rg` (used `--sdk-auth`, which the CLI flagged as deprecated but still functional — worth using the non-deprecated equivalent if this is redone for the production SP later).
    5. **Credential-exposure incident, caught and corrected in-session:** the SP's `clientSecret` was pasted directly into this chat by accident (partial JSON output from the creation command). Treated as compromised per the same discipline as any secret landing anywhere it shouldn't, even mid-session and even before being stored anywhere permanent — **rotated immediately** via `az ad sp credential reset` before the secret was used for anything, and the person was walked through building the GitHub secret's JSON value in a local text editor rather than pasting the replacement into chat. No evidence the original exposed secret was ever used for anything before rotation. Worth carrying forward as the same category of lesson as Phase 2.8's "verbally confirmed done ≠ actually done" — here it's "generated in a terminal ≠ stays out of chat" — a real gap in how service-principal-creation instructions were phrased (should have piped output straight to a variable rather than printing the full JSON to a shared terminal transcript) that any future SP-creation walkthrough in this project should account for directly rather than relying on the person to catch it.
    6. **Stored as GitHub secret `AZURE_STAGING_CREDENTIALS`** in `forge-template` (one JSON blob: `clientId`/`clientSecret`/`subscriptionId`/`tenantId` — Deploy Agent's GitHub Actions workflow, when built in Phase 4, will need to parse this JSON to extract the four fields individually for `az login --service-principal`, since it arrives as one secret rather than four).
    7. **SP access verified directly, not just assumed from the role-assignment success message:** logged in as the SP (`az login --service-principal`, via a `Read-Host -AsSecureString` prompt rather than the password on the command line, to avoid a second chat-exposure and to keep it out of PowerShell history), then ran `az containerapp env show --name forge-staging --resource-group forge-build-rg` — returned environment details successfully, confirming real, working Contributor access before Claude Code builds anything against it. Logged back in as the person's own account afterward.
  - **`team/config.yaml` updated:** `container_apps.staging.environment` set to `forge-staging`, new `resource_group: forge-build-rg` field added (comment matching the file's existing style). `production` block and the rest of the file left untouched — comments preserved throughout, no unrelated rewrite.
  - **Local tooling confirmed:** Docker Desktop installed and running (`docker ps` returns cleanly after starting the daemon — it wasn't running on first check, same "installed but not started" gap as nothing-in-particular, just a normal one-time thing to catch before relying on it for real builds).
  - **Final implementation brief handed to Claude Code this chat** (not yet built/verified — that's next):
    - New files: `core/agents/deploy_agent.py`; `core/agents/templates/dockerfiles/dotnet-web.Dockerfile.template`, `dotnet-worker.Dockerfile.template`, `nextjs.Dockerfile.template` (core layer, locked, same governance as everything else in `core/agents/`).
    - Entry point: `python -m core.agents.deploy_agent --issue-number <n> --request-id <id> --repo-path <path> --commit-sha <sha> --pr-number <n> [--dry-run]`.
    - Flow: detect deployable units (per the multi-unit detection logic above) → generate missing Dockerfiles from templates → `docker build` each, tagged `<request-id>-<unit-slug>:<commit-sha>` → `docker login`/`push` to ACR (existing `ACR_LOGIN_SERVER`/`ACR_USERNAME`/`ACR_PASSWORD` secrets, unchanged) → `az login --service-principal` via the parsed `AZURE_STAGING_CREDENTIALS` fields → `az containerapp create` (first deploy for a unit) or `update` (subsequent) per unit, against `container_apps.staging` values from `team/config.yaml` → collect FQDNs → one PR comment (reuse existing `post_pr_comment()`, no new `github_helper.py` function needed) listing each unit's staging URL, plus the design.md-mismatch flag if triggered.
    - No label applied on success — Document 6's Label Reference table has no deploy-stage label; staging isn't gated, matching Build Plan 4.7/Document 2 §4.8's framing of staging as a verification step, not a release.
    - `--dry-run`: real `docker build`/`push` (same "exercise the real tool, skip only the posting" pattern as QA/Security dry-runs), but print planned `az containerapp` commands instead of executing them (there's no real `az containerapp --dry-run` flag to lean on), post nothing.
    - Same try/except-then-reraise error handling as `security_agent.py`/`qa_agent.py`: best-effort failure comment to the tracking issue before re-raising.
    - No new pip packages — Docker CLI and Azure CLI are both external binary installs, same category as Security's Gitleaks/Dependency-Check. `CLAUDE.md` install notes (including the 32-bit-vs-64-bit MSI lesson above) to be added once Claude Code confirms the build environment matches.
    - **Explicitly deferred, not built this chat:** production path (second SP scoped appropriately depending on the resource-group-split decision above, GitHub Environment approval gate, `az containerapp update` against `forge-production`), rollback (redeploy prior tag), Phase 4 GitHub Actions wiring for either environment, and actually resolving the `EmailWorker` design.md gap (Deploy Agent surfaces it, doesn't fix it).
  - Next: hand the brief above to Claude Code to actually build and smoke-test `deploy_agent.py` and the three Dockerfile templates against `REQ-2026-01`, most likely as a `--dry-run` first (real `docker build`/push, planned-not-executed `az containerapp` calls) before a real staging deploy. Start a fresh chat for that build work, pasting in this context doc (v36).

---

- **2026-08-05 (chat 34 — this chat):** Phase 3, Step 3.10 — Deploy Agent built, smoke-tested, and real-run verified against `REQ-2026-01`, per chat 33's brief. Handed to Claude Code as a single detailed build prompt; several real findings surfaced along the way, all resolved or explicitly parked before the session closed.

  - **`team/config.yaml` gap caught before the build started:** Claude Code flagged that `container_apps.staging`'s documented schema (Document 6 v3) includes a `subscription_id` field that was never actually populated in the live file — chat 33's config update only added `environment`/`resource_group`. Decision: add the **real, already-known value** (`6ca6b79c-4e1d-477a-9b81-b91bf5ce5088`, the subscription verified live in chat 33), not a placeholder, scoped to the `staging` block only (`production` left untouched — same boundary as chat 33). Deploy Agent reads this value and passes it explicitly via `--subscription` on every `az containerapp create`/`update` call, rather than relying implicitly on whatever subscription context `az login --service-principal` lands in — a defensive measure so a future SP-scope or subscription change fails loudly instead of silently deploying to the wrong place.

  - **Local clone naming non-issue:** Claude Code used a local checkout named `forge-demo-apps-clone` rather than `forge-demo-apps`. Confirmed this doesn't matter — `--repo-path` doesn't care about folder naming, only that the clone points at the real `Flamespiker/forge-demo-apps` remote, is checked out clean (post-fix-infra-PR state, matching what QA/Security's real runs used), and that `--commit-sha` matches `git rev-parse HEAD`. All three confirmed before proceeding.

  - **Credential handling — same discipline as chat 33's exposure incident, applied cleanly this time:** Deploy Agent's dry-run needs a real `az login` to check whether each Container App already exists, which meant getting `AZURE_STAGING_CREDENTIALS` (the chat-33 JSON blob) into local `.env`. Claude Code proactively flagged the chat-33 SP-secret-exposure precedent and asked how to handle it rather than assuming; user added the value directly via their own text editor (never pasted into chat), single-quoted on one line so the embedded JSON's double quotes survive `.env` parsing. No exposure this time.

  - **`deploy_agent.py`'s multi-unit detection worked as designed on real output:** three units detected against the real merged PR #5 checkout — `req-2026-01-document-api` (web), `req-2026-01-email-worker` (worker, no ingress), `req-2026-01-frontend` (frontend, later parked). Real `docker build`/push succeeded for all three against `forgedemoacr.azurecr.io`; printed (not executed) `az containerapp` commands were correct — `DocumentApi` gets `--target-port 8080 --ingress external`, `EmailWorker` correctly gets neither, `--registry-password` redacted throughout, staging config pulled correctly from `team/config.yaml`.

  - **Two real bugs found and fixed in `deploy_agent.py` itself during the dry-run:**
    1. An em-dash console-encoding issue — same class of Windows-console bug already seen in prior stages.
    2. **The design.md-gap flag's matching logic was broken:** a literal camelCase-substring check (looking for the bare identifier `EmailWorker`/`DocumentApi`) against `design.md`, which uses spaced prose (`"Email Worker"`, `"Document API"`) — produced false-positive flags on **both** units. Fixed to check spaced-prose forms case-insensitively, with an explicit "still flag if no match found" fallback preserved (so a genuine future gap would still be caught) rather than removing the check. Re-verified clean (0 gaps) against both units after the fix.

  - **The chat-28 "`EmailWorker`-not-in-`design.md`" finding — the whole premise the flag/brief was built around — turned out to be wrong, not just fixed by the matching-logic correction.** `git blame` on `design.md` confirms every `EmailWorker` reference (C4 box, container table row, Service Bus decoupling row, monitoring row, dedicated "5. Email Worker Component" section) traces to a single commit — `6f665629`, the Design Agent's original PR #4 merge, 2026-07-29 20:29:11 -0600, `forge-pipeline[bot]` — with no later edit to the file at all. **This was a misread from the start in chat 28, not a regression documentation failed to catch up on.** Retracted the standing "Design Agent artifact-quality gap" note accordingly (see the correction inline under both the chat-28 and chat-33 entries above). The flag mechanism Deploy Agent now has is still a genuine, worthwhile generalizable check for future units/stages — it just wasn't closing a real gap in this specific case.

  - **Real (non-dry-run) deploy against `REQ-2026-01`, backend units:**
    - Both images pushed to ACR; both Container Apps created for real in `forge-staging`.
    - PR comment posted: `https://github.com/Flamespiker/forge-demo-apps/pull/5#issuecomment-5197961121`.
    - **`DocumentApi` confirmed live:** `https://req-2026-01-document-api.yellowmeadow-894377a9.canadacentral.azurecontainerapps.io` — responds in ~0.3s (404 on `/` and `/swagger`, both expected: no root route, Swagger is dev-only).
    - **`EmailWorker` deployed but crash-looping:** `ServiceBusClient` fails at startup — no connection string configured anywhere. Deploy Agent has no mechanism at all (as scoped) for wiring secrets/connection strings into a Container App. Real, previously-undiscovered architecture gap — tracked as a new open item above, not fixed this chat.

  - **Frontend unit — real, previously-undiscovered app bugs found, one fixed, one parked:**
    1. `npm run build` (the first time any stage has run a real Next.js type-check — QA's `npm test` only ever exercised Jest) failed on `frontend/components/Navigation.tsx:19`: a custom `NavItem.icon` prop type narrowed `aria-hidden` to plain `boolean`, incompatible with `lucide-react`'s icon components, which inherit React's own `AriaAttributes` typing (`boolean | "true" | "false"`). **Fixed** (widened to match React's own contract) and opened as `forge-demo-apps#8` — reviewed and merged directly by the user (not auto-merged; Claude Code correctly flagged the merge as unexpected from its own session view before confirming it was an out-of-band user action, not a process error).
    2. A second, unrelated failure followed immediately: `lib/app-insights.ts:70` — `@microsoft/applicationinsights-core-js` resolves at two different `node_modules` paths (top-level vs. nested inside `applicationinsights-analytics-js`), so TypeScript sees two structurally-identical-but-nominally-distinct `ITelemetryPlugin`/`ITelemetryItem` types and rejects the assignment. **Deliberately not investigated or fixed this chat** — a dependency-resolution problem is a different, riskier class of change than a one-line type annotation, and this was already the second app-level bug found in one session; decided to stop and let the user look at it separately rather than have Claude Code propose a third autonomous fix in a row. Frontend containerization/deploy remains blocked on this.

  - **Housekeeping backfilled while in the file anyway:** `.env.example` had no ACR/Azure entries — added. `CLAUDE.md`'s "Files created" list was missing `security_agent.py` (should have been added at Step 3.9) as well as `deploy_agent.py` — both backfilled.

  - **PRs opened this chat:**
    - `forge-template#3` (`deploy_agent.py` + templates + `CLAUDE.md`/`.env.example` updates) — reviewed by the user directly, **merged** (`93e34fb`). Merge verified as a clean match against the last-reported diff (`git diff` against the reported commit returned empty; branch history confirmed as exactly the two reported commits on top of the expected base).
    - `forge-demo-apps#8` (`Navigation.tsx` fix) — **merged** (`d92b6a7`), by the user directly.

  - **Azure CLI identity note:** the real run left the local `az` session authenticated as the `forge-deploy-staging` service principal. User logged back into their own account via `az login --use-device-code` (same MFA-flow fix already logged from chat 33's Azure setup — the default interactive flow still doesn't surface a proper MFA prompt in this environment) and confirmed via `az account show` (subscription "Mikes Sub", user `mike.faulkner@outlook.com`) before the session closed.

  - **Session outcome:** Step 3.10 (Deploy Agent) is built, merged, and real-run verified for two of three units. `DocumentApi` is live and confirmed responding in `forge-staging`. `EmailWorker` deploys but needs a secrets-wiring design decision before it can actually run. Frontend is blocked on an unresolved dependency bug, unrelated to Deploy Agent itself. Both new gaps are tracked as open items above, not started.

  - Next: pick up the two new open items (`EmailWorker` secrets/connection-string wiring; the `app-insights.ts` dependency-duplication fix) in a future chat — neither was started this chat. Otherwise, Phase 3's remaining stage-by-stage build work is complete; Phase 4 (GitHub Actions orchestration wiring) is the next major phase, pending a decision on when to pick it up.

---

- **2026-08-06 (chat 35 — this chat):** Phase 4 — Pipeline Wiring. All seven workflow stubs from Phase 1 wired for real; a genuine cross-repo architecture gap discovered and closed mid-session; dispatch chain verified end-to-end via a manual test against the existing (closed) PR #5.

  - **Built and committed to `forge-template` main** (commit `8a702ee7524c8b513e5bc07c63aaa92e7cf1ad03`, direct-to-main — same convention as prior infra/orchestration fixes, not application code requiring a PR):
    - `00-intake.yml`, `01-requirements.yml`, `02-design.yml`, `03-implementation.yml`, `06-deploy.yml` — label-triggered, each with a real guard clause, request-id resolution, and label-lifecycle cleanup (each stage now clears the label(s) that gated its own trigger once its work is confirmed committed/posted — closes the `clarification-pending`-still-present-at-QA gap flagged in chat 31).
    - `04-qa.yml` / `05-security.yml` — trigger via `repository_dispatch` (see cross-repo gap below), run in parallel on PR-open (not on merge — QA/Security gate *before* merge, per the chat-31 timing finding), install real toolchains (dotnet/node for QA; gitleaks/dependency-check/java for Security).
    - `core/agents/create_ado_items.py` (new) — driver wiring `ado_helper.py`'s `create_epic`/`create_feature`/`create_user_story` calls to `ado-work-items.json`. Per the standing Phase 4 design recommendation (chat 30/31): **fails the whole workflow run hard on any single ADO item creation error**, so Design can never start against a partial traceability chain. Writes real IDs + `primary_user_story_id` back on success.
    - `core/agents/workflow_glue.py` (new) — attachment download, request-id resolution, PR↔tracking-issue resolution in both directions. Verified live against real data: correctly resolved `REQ-2026-01` from issue #2, and PR #5 ↔ issue #2 both directions.
    - `github_helper.py` gained `get_issue()`.
    - Live agent source re-checked against `CLAUDE.md` before writing each workflow, per the standing "verify against live repo" principle — no discrepancies found this time.

  - **Cross-repo trigger gap found and closed — candidate for its own ADR (ADR-0012, not yet written):** `04-qa.yml`/`05-security.yml` need to react to PR events in `forge-demo-apps`, but the workflow files live in `forge-template` — GitHub Actions cannot natively trigger a workflow in one repo off an event in another. Document 2 already stated the intended architecture ("triggering webhooks come from the monorepo") but no notifier had been built. **Decision: `repository_dispatch` from `forge-demo-apps`, not polling** (polling would be a disguised long-running orchestrator service, contradicting the existing "GitHub Actions *is* the orchestrator" principle) and not `workflow_dispatch` (manual — defeats Phase 4's purpose).
    - Built `notify-forge.yml` in `forge-demo-apps` (pushed via `gh api`, since the App itself lacks `workflows` permission) — forwards `feature/*` PR events to `forge-template` as a `repository_dispatch` with payload `{pr_number, head_sha, head_ref}`.
    - **Required extending the `forge-pipeline` App's installation to cover `forge-template` as well as `forge-demo-apps`** (previously `forge-demo-apps`-only, per Phase 2.1's "not org-wide" deliberate scoping) — done via the GitHub UI (`/settings/installations` → Configure → add repository; API-only wasn't sufficient for this specific change). Confirmed both repos resolve under installation `148876680` with identical permissions.
    - **`FORGE_APP_CLIENT_ID` (repo variable) and `FORGE_APP_PRIVATE_KEY` (repo secret) copied to `forge-demo-apps`** at the repo level (not inside the `staging`/`production` Environment scopes) — `notify-forge.yml` runs as a plain job outside any Environment context, so it can only see repo-level values.
    - **Architecture note carried forward, not yet resolved:** this puts a small piece of orchestration plumbing in `forge-demo-apps`, slightly complicating the "core layer only lives in `forge-template`" framing from Document 2. Thin and justified (a monorepo can't emit a cross-repo webhook from the other side), but a real deviation worth its own ADR rather than an undocumented exception — **ADR-0012 not yet written, flagged for the next doc-update chat.**

  - **Dispatch chain verified end-to-end**, deliberately without touching PR #5 (already merged/closed) or re-triggering `03-implementation.yml`/the Managed Agents coordinator (out of scope both for cost and for risk of disturbing a closed PR):
    - First attempt (before committing): dispatch fired, GitHub returned 204, but **zero effect** — root-caused, not assumed: `repository_dispatch` only matches workflow definitions already on the repo's **default branch**, and the Phase 4 rewrite still existed only as uncommitted local changes at that point. Confirmed via `git show` against `main` showing the old `workflow_dispatch`-only stub content.
    - After commit/push (`8a702ee7...`): re-fired the identical dispatch payload. Both runs (QA `31068863663`, Security `31068863654`) show `repository_dispatch` → `conclusion: success`.
    - Guard-clause behavior confirmed correct from actual run logs, not assumed: both jobs correctly parsed `pr_number`/`head_sha`/`head_ref` from the payload, called `get_pr()`, saw PR #5's real state (`closed`), and correctly skipped every downstream step (checkout, toolchain setup, agent invocation, label cleanup) rather than running against stale/merged content. Nothing was posted to PR #5 — its last comment is still the chat-34 deploy-stage one.
    - **What this proves:** the full chain works — `notify-forge.yml`'s payload shape → `repository_dispatch` crossing the repo boundary → correct guard-clause parsing and decision logic on the `forge-template` side.
    - **What this does not prove:** a real `qa_agent.py`/`security_agent.py` invocation succeeding *through the dispatch path specifically* (as opposed to invoked directly, which chats 31/32 already verified) — that requires an actually-open PR, deliberately out of scope this session. **Decision: not pursued** — a throwaway test PR was considered and explicitly declined as disproportionate to what remained unverified (the payload/permissions risk, the actual unknown, is now resolved).

  - **Design question raised and resolved, not a gap:** `06-deploy.yml` deploys from the feature PR's HEAD commit, not a merged one, since nothing currently guarantees merge has happened by the time both `qa-approved`/`security-approved` labels land. **Confirmed intentional, no code change** — consistent with Document 2 §4.8's existing framing of staging as "a verification step, not a release" (no label applied on deploy success, staging not gated the way production is). The underlying question of whether staging should require PR review/approval (not just both gate labels) before deploying was raised explicitly and deferred to Mike's risk tolerance — left as-is, not built.

  - **Cost note:** this entire phase was near-zero Anthropic spend — wiring is YAML/orchestration work, not new agent code, and the dispatch verification real-run costs (QA ~$0.024, Security ~$0.006 per the existing cost log) were the only Claude-call exposure. The one deliberately avoided cost driver was any re-trigger of `03-implementation.yml`'s Managed Agents coordinator session, which remains the single most expensive stage per-invocation and was never fired this session.

  - **Not done this session (explicitly deferred):**
    - **4.8 — branch protection rules** on `forge-demo-apps` `main` (require PR reviews, require the `security-check` status check, no direct pushes). Manual GitHub settings work, not build work — next chat.
    - **4.10 — full dry-run walkthrough** (trigger a pipeline with a dummy spreadsheet, walk every stage manually confirming labels/comments/artifacts, including confirming the Claude Console per-subagent audit trail is reachable for Stage 3). Not started — best done as its own session once 4.8 is in place.
    - **ADR-0012** (cross-repo dispatch architecture) — not yet written. Candidate for a dedicated doc-update chat, same one-document-per-chat convention as other ADRs/doc updates.
    - Document 6's Label Reference table gap (`clarification-pending`, `design-approved` missing) and the Build Plan's missing label-creation step — both still outstanding from chat 26, untouched this session.
    - `FORGE_Build_Plan_v4.md` — checkboxes updated this session (see below) to reflect Phases 1–3 and Phase 4 steps 4.1–4.7/4.9 as complete; 4.8/4.10 left unchecked. Produced `FORGE_Build_Plan_v5.md`.

  - Next: **Phase 4, step 4.8** (branch protection on `forge-demo-apps` `main`) in a fresh chat — start there, then 4.10's full dry-run once protection is in place. After that, Phase 4 closes out and Phase 5 (App 1 greenfield validation) begins. Mike is updating `CLAUDE.md` directly this session; review it for drift at the start of the next chat, per the standing "verify against live repo/docs, not summaries" principle.

---

- **2026-08-05 (chat 36 — this chat):** Phase 4, Step 4.8 (branch protection on `forge-demo-apps` `main`) attempted — **blocked, not completed**, new open item surfaced.

  - **Spec confirmed from `FORGE_Build_Plan_v5.md` (ADR-0010 version)** before acting: require PR reviews (1 approver), require status check `security-check` (matches the check-run name Step 3.9's `create_check_run()` already produces), no direct pushes to `main`. Decision made this chat, not previously specified: **`enforce_admins: true`** — protection applies even to Mike as repo admin, no bypass for emergencies.

  - **Confirmed the `forge-pipeline` App cannot do this itself** — its permission set (Contents, Pull requests, Issues, Checks, Metadata — all R/W or R, per Document 3/7, locked, no RFC-free additions) does not include Administration, so branch protection has to be applied from Mike's personal `Flamespiker` account via `gh api`, not through the App's installation token. This is a real, permanent constraint, not a one-off manual-step framing — worth keeping in mind for any future automation of this step.

  - **New blocker found, not previously known:** `gh api --method PUT repos/Flamespiker/forge-demo-apps/branches/main/protection` returned `403` — `"Upgrade to GitHub Pro or make this repository public to enable this feature."` Confirmed via web search this is a genuine GitHub platform constraint: classic branch protection on **private** repos requires GitHub Pro (or Team/Enterprise) on a personal account; free personal accounts only get it on public repos. `forge-demo-apps` is private on the free `Flamespiker` account, so the rejection is expected, not a misconfigured payload.

  - **Three options surfaced and discussed:** (1) upgrade to GitHub Pro (~$4/mo, no architecture impact), (2) make `forge-demo-apps` public, (3) migrate to a GitHub Team-plan organization (bigger change, likely closer to what production will actually look like, but overkill just to unblock one build-phase step). Mike initially leaned toward making the repo public, prompted to first run a full-history Gitleaks scan (`gitleaks detect --source . --log-opts="--all"`) as a pre-visibility-change safety check given the irreversible nature of a repo's history going public — **then changed his mind before running it or changing visibility.** No visibility change was made; `forge-demo-apps` remains private.

  - **Decision: 4.8 deferred**, pending Mike's decision on which of the three options to take. No branch protection is in place on `forge-demo-apps` `main` as of this session — direct pushes and unreviewed merges are still possible. `branch-protection.json` (the JSON payload for the intended API call, described above) was drafted but not committed anywhere (correctly kept as an untracked scratch file, not part of the repo).

  - **Not done this session:** the branch protection rule itself; the pre-visibility-change Gitleaks history scan (not run, since the public-repo path was abandoned); 4.10 (full dry-run), which was already gated on 4.8 being complete first.

  - Next: **Mike to decide** between GitHub Pro / public repo / org migration for `forge-demo-apps`, then retry Step 4.8 in a fresh chat with this context doc (v39). Once 4.8 is genuinely complete, proceed to 4.10 (full dry-run walkthrough). Build Plan (`FORGE_Build_Plan_v5.md`) intentionally left unchanged this session — 4.8 checkbox stays unchecked since the step did not complete.

---

- **2026-08-05 (chat 37 — this chat):** Phase 4, Step 4.8 — **resolved and complete.** Mike upgraded to GitHub Pro; the same `branch-protection.json` payload and `gh api PUT .../branches/main/protection` command from chat 36 (no changes) succeeded.

  - **Verified live** via the API's own response (not assumed from the 200 status alone): `required_status_checks.checks` shows `security-check` scoped to `app_id: 4388813` (the `forge-pipeline` App) — GitHub auto-scoped the required check to that specific App's check runs, tighter than the Build Plan literally specified, no downside. `required_pull_request_reviews.required_approving_review_count`: 1. `enforce_admins.enabled`: true (per Mike's chat-36 decision — no admin bypass). `allow_force_pushes`/`allow_deletions`: both false.
  - **`forge-demo-apps` remains private** — Mike's earlier lean toward making it public (chat 36) was abandoned in favor of the GitHub Pro upgrade, which was always the least-disruptive of the three options discussed. No repo visibility change occurred; no Gitleaks full-history scan was needed.
  - **Standing note carried forward:** classic branch protection on private repos only stays enforced while the account remains on a paid tier (Pro/Team/Enterprise). If Pro is ever downgraded back to Free while `forge-demo-apps` stays private, this protection will likely stop being enforced even though the settings may still display as configured — worth remembering before any future cost-cutting pass touches GitHub subscriptions.
  - **Build Plan updated:** `FORGE_Build_Plan_v5.md` → `FORGE_Build_Plan_v6.md`, 4.8 checked off with the resolution details noted inline. Only step 4.10 (full dry-run) remains unchecked in Phase 4.
  - Next: **Phase 4, Step 4.10** — full dry-run walkthrough (trigger a pipeline with a dummy spreadsheet, walk every stage manually confirming labels/comments/artifacts appear correctly, no deployment; for Stage 3, also confirm the Claude Console per-subagent audit trail is reachable from the coordinator session). Its only dependency (4.8) is now satisfied — start this in a fresh chat with context doc v39 (or later) and `FORGE_Build_Plan_v6.md`. Once 4.10 passes, Phase 4 closes out and Phase 5 (App 1 greenfield validation) begins.

---

## How to Use This Document

- **Starting a new chat:** Tell Claude "Continue the FORGE project — read the context doc first" and point to this file.
- **After each chat:** Ask Claude to append a session note with any new decisions made. Update version number.
- **If a decision changes:** Update the relevant section and note the change date inline.
